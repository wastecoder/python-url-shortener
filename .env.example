# Copy this file to .env and adjust it. .env is never committed.

# PostgreSQL DSN used by SQLAlchemy.
DATABASE_URL=postgresql+psycopg://url_shortener:url_shortener@localhost:5432/url_shortener

# Public origin the short URLs are built from, without a trailing slash.
BASE_URL=http://localhost:8000
