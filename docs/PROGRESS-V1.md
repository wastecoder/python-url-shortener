# Progresso V1 — url-shortener (corte mínimo)

Roadmap em fases para construir o escopo mínimo: criar link, redirecionar, ler os metadados do
link, e nada mais. A sequência **constrói de dentro para fora** — domínio puro, depois casos de
uso, depois a API, e só então o banco — porque é essa ordem que a arquitetura hexagonal torna
possível, e porque é ela que produz a melhor demonstração do projeto: na Fase 4 o adaptador de
saída é trocado e o diff da camada web sai vazio.

O que ficou de fora de propósito está em [`PROGRESS-V2.md`](PROGRESS-V2.md).

**Se o tempo apertar, corte nesta ordem:** deduplicação (Fase 4), depois `GET /links/{code}`
(Fases 3 e 4). **Não corte:** os três testes com Testcontainers, o CI, e o README com a tabela de
decisões — funcionalidade não é o que está sendo avaliado neste projeto.

> **Como usar:** ao concluir um item, marque a caixa **e** risque o texto:
> `- [x] ~~item concluído~~`. Em seguida, **acrescente subitens em negrito descrevendo o que de
> fato foi feito** — decisões tomadas, nomes de módulos e classes, versões, e por quê. Todo item
> concluído deve ter:
>
> - um subitem **`Verificado:`** com a evidência concreta (o comando que rodou verde, a resposta
>   HTTP observada, o `git diff --stat` do commit);
> - quando útil, um subitem **`Fora deste item:`** dizendo o que ficou para depois, e
>   **`Caveats:`** para as armadilhas descobertas no caminho.
>
> Mantenha este arquivo em dia com a realidade. Um item marcado sem subitens não conta como
> concluído.

## Objetivos de aprendizado

O projeto existe para ensinar estes pontos. Marque quando você conseguir **explicar o item sem
consultar nada**. Esta lista é também o roteiro da entrevista.

> **As caixas continuam vazias de propósito.** A Fase 7 não as marcou: o critério é "explicar sem
> consultar", que só você consegue avaliar. O que a fase acrescentou foi a linha `→` sob cada item,
> dizendo **onde a resposta vive hoje** — para o ensaio ser conferir, e não procurar.

- [ ] Layout `src/` e por que ele impede o import acidental do pacote não instalado (Fase 0)
  → `pyproject.toml` (`[tool.hatch.build.targets.wheel]`), `tests/unit/test_package.py`, [`DEVELOPMENT.md`](DEVELOPMENT.md#3-comandos)
- [ ] uv: `sync`, `run`, grupos de dependência, e o que o lock garante que o `requirements.txt` não garante (Fase 0)
  → `pyproject.toml` (`[dependency-groups]`), `uv.lock`, `Dockerfile` (`uv sync --locked` nas duas camadas do estágio `builder`; o `runtime` não tem uv), [`DEVELOPMENT.md`](DEVELOPMENT.md#7-a-imagem)
- [ ] O que `mypy --strict` cobra que a anotação sozinha não cobra (Fase 0)
  → `pyproject.toml` (`[tool.mypy]`, `files` com `tests` e `migrations`), `tests/fakes.py` (bloco `TYPE_CHECKING`), [`TESTS.md`](TESTS.md#6-fakes-e-não-mocks)
- [ ] Regra de dependência verificada por ferramenta e não por convenção — o que o `import-linter` prova (Fase 0)
  → `.importlinter`, [`ARCHITECTURE.md`](ARCHITECTURE.md#3-os-quatro-contratos-de-dependência), [ADR-0004](adr/0004-fronteira-sem-objeto-de-dominio.md)
- [ ] Dimensionamento do espaço de chave: `62^6` versus `62^7`, e por que 7 e não 6 (Fase 1)
  → `domain/service/base62.py` (`MAX_ID`), [`CHALLENGE.md`](CHALLENGE.md#4-a-origem-o-capítulo-8-do-alex-xu), [ADR-0002](adr/0002-base62-sobre-a-sequence.md)
- [ ] As duas famílias de geração de código — hash com resolução de colisão versus id convertido para base 62 — e o trade-off entre elas (Fase 1)
  → [ADR-0002](adr/0002-base62-sobre-a-sequence.md), [`CHALLENGE.md`](CHALLENGE.md#4-a-origem-o-capítulo-8-do-alex-xu)
- [ ] Dataclass frozen versus Pydantic, e por que Pydantic não entra no domínio (Fase 1)
  → `domain/model/`, `.importlinter` (contrato `domain-is-pure`), [`ARCHITECTURE.md`](ARCHITECTURE.md#1-visão-geral-hexagonal)
- [ ] Validação de destino como superfície de ataque: `file://`, `localhost`, IP de rede privada (Fase 1)
  → `domain/service/url_policy.py`, [`SECURITY.md`](SECURITY.md#2-o-que-a-política-de-destino-recusa), `tests/unit/test_url_policy.py`
- [ ] `typing.Protocol` versus ABC, e por que Protocol é o que impede o adaptador de importar o `application` (Fase 2)
  → `application/port/`, [`ARCHITECTURE.md`](ARCHITECTURE.md#1-visão-geral-hexagonal), `tests/fakes.py`
- [ ] Fake versus mock, e o caso concreto em que um mock faz o teste provar nada (Fases 2 e 5)
  → `tests/fakes.py`, [`TESTS.md`](TESTS.md#6-fakes-e-não-mocks), `tests/integration/test_deduplication.py`
- [ ] Injeção por construtor sem framework de injeção de dependência (Fase 2)
  → `adapter/config/dependencies.py`, [`ARCHITECTURE.md`](ARCHITECTURE.md#9-composição-e-configuração)
- [ ] Por que este projeto é síncrono e não assíncrono, e qual é o erro clássico do FastAPI com `async def` (Fase 3)
  → [`ARCHITECTURE.md`](ARCHITECTURE.md#4-modelo-de-execução-síncrono-de-propósito), `main.py` (o único `async def`), `CLAUDE.md`. **Único objetivo sem ADR e sem teste próprio** — ver a lista honesta abaixo
- [ ] 301 versus 302, e o que o 301 custa em medição e em controle (Fase 3)
  → [ADR-0001](adr/0001-redirect-302.md), [`README.md`](../README.md#o-fluxo), [`API.md`](API.md#12-get-code--seguir-o-código), `tests/unit/test_redirect_endpoint.py`
- [ ] Por que o default do `RedirectResponse` é 307 e por que 307 está errado aqui (Fase 3)
  → `adapter/web/redirect_controller.py` (o `status_code=` explícito), [ADR-0001](adr/0001-redirect-302.md), [`API.md`](API.md#12-get-code--seguir-o-código)
- [ ] Rota catch-all na raiz, ordem de registro, e a mesma questão vista pelo lado do gerador — os códigos reservados (Fases 1 e 3)
  → `main.py` (`create_app`), `domain/service/url_policy.py` (`RESERVED_CODES`), [`ARCHITECTURE.md`](ARCHITECTURE.md#6-fluxo-de-redirect-get-code), `tests/unit/test_app_routes.py`
- [ ] Problem Details (RFC 7807), e por que 400 e 422 são coisas diferentes (Fase 3)
  → [ADR-0006](adr/0006-envelope-de-erro-problem-details.md), [`API.md`](API.md#2-erros-rfc-7807), `adapter/web/handler/`
- [ ] Condição de corrida em check-then-act, e por que só a constraint fecha a janela (Fase 4)
  → `application/usecase/create_link_use_case.py`, [`README.md`](../README.md#o-fluxo), [`ARCHITECTURE.md`](ARCHITECTURE.md#5-fluxo-de-criação-post-links), `tests/integration/test_deduplication.py`
- [ ] Por que o índice único é sobre `sha256(url)` e não sobre a URL — o limite de tamanho de entrada em btree (Fase 4)
  → `domain/service/url_hash.py`, [`ARCHITECTURE.md`](ARCHITECTURE.md#7-modelo-de-dados), `migrations/versions/cee192947f04_create_link_and_click.py`
- [ ] Por que a `UNIQUE` em `code` é rede de segurança e não mecanismo, e por que ainda assim ela fica (Fase 4)
  → `adapter/persistence/entity/link_entity.py`, [`ARCHITECTURE.md`](ARCHITECTURE.md#7-modelo-de-dados), [ADR-0002](adr/0002-base62-sobre-a-sequence.md)
- [ ] Escrita no caminho de leitura: contenção de linha, e por que `click` é append-only (Fase 4)
  → [`README.md`](../README.md#o-fluxo), [`ARCHITECTURE.md`](ARCHITECTURE.md#7-modelo-de-dados), `tests/integration/test_click_is_recorded.py`
- [ ] Teste que verifica estado do banco versus teste que verifica retorno de função (Fase 5)
  → [`TESTS.md`](TESTS.md#8-os-testes-que-carregam-o-projeto), `tests/integration/conftest.py` (a engine de asserção, separada)
- [ ] `services:` do GitHub Actions versus container subido pelo próprio teste (Fase 5)
  → `.github/workflows/ci.yml` (**não** declara `services:`), `tests/integration/conftest.py`, [`TESTS.md`](TESTS.md#9-os-dois-gates-de-cobertura)
- [ ] Quais peças do desenho do Alex Xu foram tiradas e por quê — Snowflake, Redis, bloom filter, sharding (Fase 7)
  → [`CHALLENGE.md`](CHALLENGE.md#4-a-origem-o-capítulo-8-do-alex-xu)
- [ ] O que ficou de fora de propósito, e o que cada corte teria custado (Fase 7)
  → [`CHALLENGE.md`](CHALLENGE.md#5-o-que-ficou-de-fora-e-por-quê), [`PROGRESS-V2.md`](PROGRESS-V2.md)

### O que faltou, honestamente

Três lacunas encontradas ao montar o mapa acima. Nenhuma invalida um objetivo; todas são pontos em
que a defesa se apoia em prosa e não em algo que fique vermelho.

1. **O modelo síncrono é o único item da lista sem ADR e sem teste.** Ele vive no `CLAUDE.md` e
   agora no `ARCHITECTURE.md`, e nada no repositório reprova se um `async def` aparecer num
   controlador. É a decisão mais fácil de quebrar em silêncio, porque quebrá-la exige mudar as
   assinaturas das portas — e um `async def` isolado num controlador *compila*. Escrever a ADR foi
   avaliado nesta fase e **descartado por decisão do usuário**; um contrato de `import-linter` não
   consegue expressá-la, então o candidato natural seria um teste que varre `adapter/web` por
   `async def`.
2. **A lista de códigos reservados não tem chamador em `src/`.** `is_reserved_code` e
   `ReservedCodeError` existem e são testados, e nenhuma linha de produção os usa — o que está
   correto e documentado (com sete caracteres fixos, colisão é impossível), mas significa que a
   metade "códigos reservados" do objetivo é defendida por argumento e não por comportamento.
3. **A metade "no CI" de três critérios de fase segue não verificada**, porque depende do `push` e
   do pull request, que são passos manuais fora deste repositório. Vale para as Fases 5, 6 e 7.

## Fase 0 — Fundação do repositório

- [x] ~~Ambiente: instalar o uv e registrar o caveat da pasta sincronizada em nuvem~~
  - **uv 0.12.7** via `winget install --id=astral-sh.uv`, sobre o **Python 3.13.7** que já estava na máquina. O brief pedia 3.12; toda a stack roda em 3.13 sem asterisco, e instalar um segundo interpretador seria custo sem retorno.
  - **O caveat ficou registrado no `CLAUDE.md`, sem workaround.** A ideia original era apontar `UV_PROJECT_ENVIRONMENT` para fora da árvore, e foi descartada: a variável é global, e um caminho absoluto faria todos os projetos uv da máquina dividirem um venv só. Se o `uv sync` falhar de forma estranha, a primeira hipótese é a sincronização travando arquivo.
  - **Verificado:** `uv run python -V` devolveu `Python 3.13.7`.
- [x] ~~`git init` com branch `main`, `.gitattributes` e `.gitignore`~~
  - **`.gitignore`** cobre `.venv/`, os caches de ferramenta, `.env` e **`docs/learning/`**. **`.gitattributes`** com `* text=auto eol=lf`, para o repositório não guardar CRLF vindo do Windows.
  - **Verificado:** o commit inicial `12efd5d` levou 13 arquivos, e `.venv/`, `docs/learning/` e `.env` ficaram de fora.
- [x] ~~Repositório no GitHub com proteção da branch `main` exigindo o check do CI~~
  - **`wastecoder/python-url-shortener`**, público desde o primeiro commit. Merge restrito a **rebase** (`allow_merge_commit` e `allow_squash_merge` desligados), para o histórico de um-conceito-por-commit sobreviver na `main` em vez de virar uma linha só.
  - **`enforce_admins: true`, e sem exigência de review.** Review é impossível de satisfazer sozinho; e sem `enforce_admins` o dono do repositório passa por cima com um clique, o que tornaria "o CI barra o merge" uma frase decorativa.
  - **Ligada depois do primeiro run:** a proteção cobra o check pelo **nome exato**, e esse nome só vira fato depois que um run o reporta (`gh pr checks 1 --json name` devolveu `check`). Exigir um nome que nenhum workflow produz tranca todo PR até alguém desligar a proteção. Não é que o PR que traz o workflow deixe de rodar: em evento `pull_request` o GitHub usa o workflow **da branch do PR**, e foi assim que o PR #1 rodou o CI com a `main` ainda sem workflow nenhum.
  - **Verificado:** o PR #2, criado de propósito com o lint quebrado, recebeu `Pull request #2 is not mergeable: the base branch policy prohibits the merge` e foi fechado sem merge. Ele fica no repositório como prova.
  - **Fora deste item:** forçar com `gh pr merge --admin` não foi tentado. Se o `enforce_admins` não estivesse valendo, o commit quebrado entraria na `main` e viraria reversão. A garantia usada é a resposta da própria API ao configurar a proteção: `"enforce_admins": true`.
- [x] ~~`pyproject.toml` com uv~~
  - **Nenhum número de versão foi digitado à mão:** as constraints foram escritas pelo `uv add` e o `uv.lock` trava o que foi resolvido — fastapi 0.141.1, starlette 1.6.0, pydantic 2.13.5, sqlalchemy 2.0.52, alembic 1.19.1, psycopg 3.3.4, pytest 9.1.1, ruff 0.16.5, mypy 2.3.1, import-linter 2.14, testcontainers 4.15.0.
  - **Sem `pytest-asyncio`**, coerente com a decisão de manter o projeto síncrono: os testes de API vão usar `TestClient`.
  - **Verificado:** `uv sync --locked` verde no runner do GitHub, que é onde essa flag importa — ela falha se o lock estiver defasado em relação ao `pyproject.toml`, em vez de resolver de novo em silêncio.
- [x] ~~Configuração de ferramenta num arquivo só~~
  - **Tudo no `pyproject.toml`:** ruff com `line-length = 100` e as regras `E,F,I,UP,B,SIM,C4,RUF`; mypy `strict` sobre `src`; pytest com o marker `integration` registrado e `addopts = -m 'not integration' --strict-markers --import-mode=importlib`; coverage com `branch = true`.
  - **`--import-mode=importlib`** dispensa `__init__.py` nas pastas de teste e elimina a colisão de nome que apareceria quando existirem `tests/unit/test_link.py` e `tests/integration/test_link.py`.
  - **Caveat:** o plugin `pydantic.mypy` **não é opcional**. Sem ele, `mypy --strict` acusa um falso `Missing named argument "database_url" for "Settings"`, porque o Pydantic v2 usa `@dataclass_transform` e o mypy sintetiza um `__init__` com todos os campos obrigatórios. Há um comentário no arquivo dizendo isso, porque removê-lo quebra o build de um jeito que não faz sentido nenhum.
- [x] ~~Esqueleto hexagonal completo, com docstring de camada por pacote~~
  - **22 pacotes**, cada `__init__.py` documentando a responsabilidade da camada e a regra de dependência dela. É o equivalente dos 21 `package-info.java` do `imagepipe`, e foi escolhido em vez de `.gitkeep` pela mesma razão: é importável, sobrevive ao Git e dá ao import-linter um módulo real em que ancorar os contratos.
  - **`inbound`/`outbound` em vez de `in`/`out`:** `in` é palavra reservada, e `application.port.in` é erro de sintaxe **no import**, não na criação — a pasta nasceria sem reclamação e explodiria só quando alguém tentasse importá-la.
  - **Decisão deliberadamente contrária à do `imagepipe`:** lá os pacotes de terceiro nível só nascem junto da primeira classe, porque ficariam meses vazios. Aqui todos estarão preenchidos até a Fase 4, e ver o mapa inteiro vale mais que descobri-lo aos poucos.
  - **Verificado:** `uv run mypy src` devolveu `Success: no issues found in 23 source files`.
- [x] ~~`.importlinter` com os três contratos~~
  - **`Contracts: 3 kept, 0 broken` — e isso, sozinho, não valia nada.** O relatório dizia `Analyzed 22 files, 0 dependencies`: sem nenhum import entre pacotes, os contratos passavam por vacuidade.
  - **Os dois tipos foram provados:** `import fastapi` dentro de `domain/__init__.py` quebra *The domain imports no framework* e sai com 1; `import url_shortener.adapter` no mesmo arquivo quebra *The dependency arrow points inward* e sai com 1. Ambos revertidos em seguida.
  - **Caveats:** `include_external_packages = True` é obrigatório para os contratos `forbidden` enxergarem pacotes de terceiros.
- [x] ~~`adapter/config/settings.py` com pydantic-settings e `.env.example`~~
  - **Nenhum campo tem default**, de propósito: `DATABASE_URL` ou `BASE_URL` faltando derruba o boot com mensagem clara, em vez de a aplicação subir feliz contra o banco errado. **`extra="forbid"`** transforma uma variável digitada errada no `.env` em erro em vez de silêncio. **`@lru_cache`** em `get_settings`, para o ambiente ser lido uma vez só.
  - **Verificado:** dois testes — um lendo do ambiente, outro afirmando que a construção falha quando `BASE_URL` some.
- [x] ~~CI magra em `.github/workflows/ci.yml`~~
  - **Seis passos, sem Docker, quinze segundos:** `uv sync --locked`, `ruff check`, `ruff format --check`, `mypy src`, `lint-imports`, `pytest`.
  - **Caveat:** `astral-sh/setup-uv@v10` **não resolve**. A action parou de publicar tag flutuante de major depois da `v7`; da `v8` em diante só existem tags exatas, e o primeiro run morreu em `Unable to resolve action` antes de qualquer passo. Corrigido pinando `@v10.0.1` — que é a prática melhor de qualquer forma. `actions/checkout@v7` continua publicando a tag de major.
  - **Caveat:** `ruff format` também processa **Markdown**, formatando os blocos de Python dentro dele. Um exemplo mal formatado no README ou num ADR derruba o pipeline; `docs/learning/` escapa porque está no `.gitignore`.
  - **Verificado:** run `33280203590` verde nos seis passos.
- [x] ~~Os três ADRs em `docs/adr/`~~
  - **Escritos antes do código e antes desta fase, junto do commit inicial**, porque são decisões já tomadas e que não vão mudar: `0001-redirect-302`, `0002-base62-sobre-a-sequence` e `0003-sem-fila`. Formato do ShopFlow: Status, Contexto, Decisão, Alternativas consideradas e Consequências, separando as positivas dos custos.
- **Critério:** ~~os cinco comandos verdes num repositório sem regra de negócio, um pull request que não consegue ser mergeado com o check vermelho, e o `lint-imports` provado não-vacuoso.~~ **Atendido.** PR #1 verde e mergeado por rebase; PR #2, quebrado de propósito, recusado pela política da branch e fechado sem merge; os dois contratos vistos quebrando e voltando.
- **Documento de aprendizado:** `docs/learning/fase-0-fundacao.md`, com o exercício em `docs/learning/exercicio_fase_0.py`.

## Fase 1 — Domínio puro

- [x] ~~`domain/service/base62.py`: `encode(id) -> str` sobre o alfabeto `0-9a-zA-Z`, com padding à esquerda até **exatamente 7 caracteres**, e `decode(code) -> int`. Funções puras, sem estado e sem I/O~~
  - **Quatro constantes, todas públicas e anotadas com `Final[...]`:** `ALPHABET` (`0-9a-zA-Z`, nessa ordem — é ela que fixa que `encode(10)` é `000000a` e não `000000A`), `BASE = len(ALPHABET)` derivado e nunca o literal 62, `CODE_LENGTH = 7` e `MAX_ID = BASE**CODE_LENGTH - 1` (`3_521_614_606_207`). O mapa reverso `_DIGIT_VALUES` é privado e construído a partir do próprio alfabeto, então os dois não podem discordar.
  - **`encode` recusa id negativo e id que não cabe em sete caracteres.** Devolver oito caracteres em silêncio quebraria a invariante que torna as rotas da API ingeráveis, e um id negativo só pode vir de bug — nenhuma sequence produz um. `encode(0)` é `0000000` e é aceito: `encode` é função numérica pura, e recusar o 0 seria enfiar regra de negócio no módulo errado.
  - **`decode` é estrito, e a ordem das checagens importa:** comprimento primeiro, alfabeto depois. Invertido, `decode("")` devolveria 0 e inventaria um id. Estrito também é o que dá a bijeção nos dois sentidos — um `decode("1") == 1` leniente teria `encode` devolvendo `0000001`, e um link passaria a ter dois nomes.
  - **Erro é `ValueError` da stdlib, nunca uma subclasse de `DomainError`.** A taxonomia de erro do projeto é fechada e cada entrada dela vira uma linha da tabela de Problem Details; falha aqui é bug (id negativo, ou a sequence além de 3,5e12), que é a linha do 500. Criar uma classe de domínio obrigaria a inventar uma linha para uma situação que nenhum cliente consegue causar.
  - **Caveat — `Final` sem subscrito desliga a checagem:** `int.__pow__` é tipado como devolvendo `Any`, então `MAX_ID = BASE**CODE_LENGTH - 1` sem `Final[int]` é inferido como `Any` e toda comparação contra ele deixa de ser verificada. Confirmado com `reveal_type` neste repositório: sem anotação e com `Final` pelado, `Any`; com `Final[int]`, `int`.
  - **Verificado:** 83 testes em `tests/unit/test_base62.py`. A tabela `KNOWN_CODES` (`0 -> 0000000`, `10 -> 000000a`, `36 -> 000000A`, `62 -> 0000010`, `56_800_235_583 -> 0ZZZZZZ`, `3_521_614_606_207 -> ZZZZZZZ`) é usada quatro vezes: valor literal, padding e alfabeto, `decode(encode(n)) == n` e `encode(decode(c)) == c`.
- [x] ~~`domain/service/url_policy.py`: aceita apenas `http` e `https`; recusa `localhost`, loopback, rede privada, link-local, faixas reservadas e URL com credencial embutida; expõe a lista de códigos reservados (`docs`, `redoc`, `openapi.json`, `health`, `links`) como rede de segurança para a Fase de alias do V2, já que com 7 caracteres fixos nenhuma delas pode ser gerada~~
  - **`validate_target_url(url) -> None`**, que levanta `InvalidTargetUrlError` ou não diz nada. **Não normaliza absolutamente nada:** a URL é guardada, hasheada e redirecionada exatamente como chegou, senão a linha do banco deixaria de conter o que o chamador mandou.
  - **A ordem das onze checagens é semântica, não estética.** Comprimento (2048) antes de tudo, porque `urlsplit` é `lru_cache` e uma string gigante ficaria presa num cache global só por ter sido olhada. Caracteres de controle **na string crua**, porque `urlsplit` apaga `\t`, `\r` e `\n` de qualquer posição — validar depois de parsear é validar uma URL diferente da que vai ser guardada. `urlsplit` e a leitura de `parts.port` dentro do mesmo `try`, porque as duas levantam `ValueError` e sem isso uma URL forjada vira 500 em vez de 400.
  - **Rota pública é `is_global and not is_multicast`, e não uma lista de faixas.** `is_private` deixaria passar o CGNAT `100.64/10`, que não é privado nem global — medido nesta máquina. Multicast é a única faixa que o `is_global` ainda chama de global, então sai na mão.
  - **`rstrip(".")` e não `removesuffix(".")`:** o ponto final é o rótulo raiz do DNS e faz o `ipaddress` recusar `127.0.0.1.`, que então passaria como nome. Com `removesuffix`, `127.0.0.1..` continuava passando; tem uma linha de teste só para essa diferença.
  - **O host é validado como host antes de qualquer coisa confiar nele.** O parser da stdlib segue a RFC 3986 e todo navegador segue o WHATWG, e os dois discordam sobre a barra invertida: `http://127.0.0.1\` tem host `127.0.0.1\` aqui e host `127.0.0.1` no navegador. Em vez de caçar cada grafia, tudo que não é um hostname (rótulos de `[a-z0-9_]`, sem hífen nas pontas) é recusado. O último rótulo ainda precisa ser um TLD plausível — letras ou `xn--` —, o que também derruba `127.1`, `0177.0.0.1` e `0x7f.0x0.0x0.0x1`, três formas que o `ipaddress` não lê e o navegador resolve para 127.0.0.1.
  - **Um IPv6 pode carregar um IPv4 dentro, em quatro grafias**, e o `is_global` só enxerga duas. `_embedded_ipv4` desembrulha `::ffff:a.b.c.d`, `::a.b.c.d`, 6to4 e o prefixo NAT64 `64:ff9b::/96` e faz a mesma pergunta ao endereço de dentro; `fec0::/10` (site-local, depreciado) sai à parte.
  - **`RESERVED_CODES` é `frozenset` e `is_reserved_code` só devolve `bool`.** Nada levanta `ReservedCodeError` no V1, de propósito: a fase pede a lista exposta e a classe existindo, não um chamador. Uma função que ninguém chama e que *parece* viva é pior do que uma exceção que ainda não tem quem a levante.
  - **Verificado:** 104 testes em `tests/unit/test_url_policy.py`, um por ramo de recusa mais os controles de aceitação que dão sentido a eles (`172.32.0.1` logo fora do `172.16/12`, `[::ffff:8.8.8.8]`, `example.com.`, punycode, `localhost.example.com`, `127.0.0.1.example.com`). Fora da suíte, uma matriz de 21 recusas e 14 aceitações rodada contra o módulo: zero divergências.
  - **Fora deste item:** resolução de nome. A política decide só pela string, o que deixa em aberto o nome que resolve para endereço interno e o DNS rebinding — está registrado como item da Fase 8 do [`PROGRESS-V2.md`](PROGRESS-V2.md). Também ficou de fora `domain/service/url_hash.py`, que é da Fase 2.
  - **Caveats:** `MAX_TARGET_URL_LENGTH = 2048` e a recusa de host não-ASCII (mande punycode) são **acréscimos** ao que a fase pediu, e estão aqui porque fecham grafia ambígua, não porque a lista mandava. O literal IPvFuture `http://[v1.x]/` não é recusado pelo parser: vira o nome `v1.x`, que é aceito como público — inofensivo, porque não resolve para lugar nenhum.
- [x] ~~`domain/model/`: `Link`, `Click` e `ShortCode` como dataclasses frozen, validando as invariantes no `__post_init__`~~
  - **Um tipo público por módulo:** `short_code.py`, `link.py`, `click.py`. Todos `frozen=True, slots=True`; `Link` e `Click` também `kw_only=True`, que é o que impede trocar `user_agent` por `referer` — dois `str | None` vizinhos — sem ninguém perceber. Em `ShortCode`, que tem um campo só, `kw_only` seria ruído.
  - **`ShortCode.from_id(link_id)` é o único ponto onde modelo e serviço se encontram**, e a seta é `model -> service`, nunca o contrário. É por causa dele que a Fase 2 não vai precisar importar `base62` em lugar nenhum: o caso de uso pede o código de um id.
  - **`Link.id` é obrigatório e `Click` não tem `id`.** A assimetria é a decisão: o id do link vem do `nextval` **antes** do insert e é o que gera o código, então um link sem id não existe; o clique é append-only e a única pergunta que se faz a ele é um `COUNT` por link, então um campo que ninguém lê só criaria mais um opcional.
  - **`Link` não carrega `url_hash`.** O hash é chave de índice de tamanho fixo — assunto de persistência — e é calculado **antes** de qualquer `Link` existir, no `SELECT` que decide se é preciso criar um. Guardar aqui seria uma segunda cópia do mesmo fato, com um `__post_init__` recalculando sha256 para conferir se as duas concordam. Consequência já fixada para a Fase 2: `LinkRepository.find_by_url_hash` recebe `str`, não um objeto.
  - **Violação de invariante levanta `ValueError`, nunca `DomainError`.** Um modelo malformado é bug de quem construiu; regra de negócio quebrada é coisa que a API precisa explicar. Se fossem a mesma família, um `except` só engoliria as duas.
  - **A regra de fuso é `utcoffset() is None`, uma condição só.** `datetime.utcoffset()` já devolve `None` exatamente quando não há `tzinfo` ou quando o `tzinfo` não sabe o próprio deslocamento — testar `tzinfo is None` junto seria um ramo inalcançável. Um instante com deslocamento `-03:00` é aceito e compara igual ao gêmeo em UTC: a regra é "nomeia um instante sem ambiguidade", não "está rotulado UTC".
  - **Verificado:** 22 testes de `ShortCode`, 11 de `Link` e 9 de `Click`, incluindo `dataclasses.replace` revalidando as invariantes na cópia, `FrozenInstanceError` na atribuição, o `tzinfo` que não sabe o próprio deslocamento, e a lista de campos afirmada nome a nome — é o teste que trava a ausência de `url_hash` e a ausência de `Click.id`.
  - **Caveats:** `Click.ip` é objeto `IPv4Address | IPv6Address`, não texto. **Esta nota carregava um palpite errado, e a Fase 4 o corrigiu:** ela dizia que o mapper "provavelmente" precisaria de `str(click.ip)` na escrita e `ip_address(row.ip)` na leitura. Não precisa de nenhum dos dois — o psycopg 3 registra loaders de `ipaddress` por padrão, então o valor lido já é um `IPv4Address` e `ip_address()` sobre ele levanta `ValueError`. A coluna é `Mapped[IPv4Address | IPv6Address | None]` e o mapper copia o objeto nos dois sentidos, sem conversão.
- [x] ~~`domain/exception/`: `DomainError` como base; `InvalidTargetUrlError` (carrega o motivo da recusa), `LinkNotFoundError` (carrega o code), `ReservedCodeError`. Nenhuma delas sabe o que é status HTTP — quem traduz é o handler da Fase 3~~
  - **`DomainError(Exception)`, e explicitamente não `ValueError`.** Se herdasse de `ValueError`, o `except ValueError` que a Fase 2 vai usar para transformar um code inválido em `LinkNotFoundError` engoliria também as regras de negócio, e o 404 responderia coisa que merecia 400. Tem um teste afirmando `not issubclass(DomainError, ValueError)`.
  - **`self.message` tipado, e não `args[0]`:** `BaseException.args` é `tuple[Any, ...]`, então um handler que devolvesse `error.args[0]` de uma função `-> str` quebraria no `mypy --strict`.
  - **`RejectionReason(StrEnum)` mora em `domain/exception/rejection_reason.py`, módulo próprio.** Se morasse dentro de `url_policy`, a exceção importaria o serviço e o serviço importaria a exceção — ciclo. Os valores são escritos à mão e hifenizados porque `auto()` num `StrEnum` devolve o nome do membro em minúsculas com underscore, e o formato de fio desta API é hifenizado.
  - **Nove motivos, uma linha só na tabela de erro.** `URL_TOO_LONG`, `FORBIDDEN_CHARACTER`, `MALFORMED_URL`, `MISSING_SCHEME`, `UNSUPPORTED_SCHEME`, `MISSING_HOST`, `CREDENTIALS_IN_URL`, `NON_PUBLIC_HOST` e `NON_PUBLIC_ADDRESS` mapeiam todos para `invalid-target-url` e 400 na Fase 3; o motivo viaja no corpo como campo de extensão. Ele é taxonomia de máquina, não uma segunda taxonomia de status.
  - **`LinkNotFoundError.code` e `ReservedCodeError.code` são `str`, nunca `ShortCode`.** O redirect é catch-all na raiz, então o valor que a exceção carrega quase nunca é um código válido — tipar como `ShortCode` tornaria irrepresentável justamente o caso que interessa, e apontaria `domain.exception` para `domain.model`.
  - **Verificado:** 24 testes em `tests/unit/test_domain_errors.py`, incluindo a asserção parametrizada de que nenhuma exceção tem `status`, `status_code`, `http_status` ou `problem_type`, e a de que os nove valores são únicos — lida por `__members__`, porque iterar o enum esconde alias e faria a asserção nunca poder falhar.
  - **Fora deste item:** as exceções não sobrevivem a `copy.copy` nem a `pickle`, porque a assinatura do `__init__` não bate com `args`. Um `__reduce__` de duas linhas resolveria; num app síncrono de um processo só, nada copia exceção, então fica como limite conhecido em vez de código sem chamador.
- [x] ~~Testes unitários escritos antes do código: `base62` com `parametrize` em 0, 1, 61, 62, um id grande, o round-trip `decode(encode(n)) == n` e o padding em 7; `url_policy` com um caso por ramo de recusa~~
  - **256 testes, todos sem Docker e sem banco**, em 0,5 s. O ciclo foi teste vermelho, implementação, verde, e só então o commit com os dois juntos — nunca um commit só de teste, porque a `main` é mergeada por rebase e um commit intermediário vermelho entraria no histórico em silêncio e quebraria o `git bisect`.
  - **Todo teste tem docstring em Given / When / Then** e assinatura `-> None`; nenhum mock, nenhum marcador, nenhum `async`. Os motivos de recusa são afirmados com `is RejectionReason.X`, não contra o texto da mensagem.
  - **A suíte foi medida por mutação, não por leitura.** Rodando mutantes contra uma cópia do repositório, sobreviveram seis: `frozen` do `Click`, a constante de comprimento máximo, a regra de que `ShortCode` não normaliza, o ramo do `tzinfo` sem deslocamento, a unicidade dos motivos e a metade morta da checagem de credencial. Os cinco primeiros viraram teste; o sexto virou simplificação — `parts.password` não pode ser lido sem `parts.username`, então a segunda metade da condição era ramo inalcançável e saiu.
  - **Verificado:** `uv run pytest` → `256 passed`. Os treze commits de código anteriores a este foram rodados um a um, cada um em seu próprio worktree: **13 verdes, 0 vermelhos**, com `ruff check` limpo em todos.
- **Critério:** ~~`uv run pytest` verde sem Docker e sem banco. `grep -rE "fastapi|sqlalchemy|pydantic|starlette" src/url_shortener/domain` não retorna nada, e o `import-linter` chega à mesma conclusão sozinho.~~ **Atendido.** `uv run pytest` → `256 passed`; `uv run mypy src` → `Success: no issues found in 33 source files`; `uv run ruff check .` e `ruff format --check .` limpos; o `grep` sai com 1 e sem saída. O `lint-imports` deixou de ser vacuoso: a Fase 0 registrava `Analyzed 22 files, 0 dependencies`, e agora são **`Analyzed 43 files, 24 dependencies. Contracts: 3 kept, 0 broken.`** — é a primeira fase em que existem arestas de verdade dentro do domínio para os contratos julgarem. `git diff --stat main..HEAD`: 19 arquivos, 1.548 inserções.
  - **Caveat sobre o próprio critério:** o `grep` só passa porque as docstrings escrevem os nomes capitalizados (`FastAPI`, `SQLAlchemy`, `Pydantic`, `Starlette`). Um `pydantic` em minúsculas em prosa faria o critério de aceitação da fase *parecer* falhar. O `-i` acha a docstring de `domain/__init__.py`; o `import-linter` é quem responde a pergunta de verdade.
  - **Revisão:** o diff passou por uma revisão adversarial em cinco lentes (segurança, correção, aderência ao spec, qualidade de teste por mutação, tipagem), com dois céticos independentes por achado. Vinte achados sobreviveram; foram corrigidos os de segurança (barra invertida, host percent-encoded, octeto hexadecimal, as quatro formas de IPv6 com IPv4 dentro), o `py.typed` ausente — sem ele, `mypy` sobre `tests/` enxerga o domínio inteiro como `Any` — e os seis buracos de teste. Ficaram registrados como limite consciente: cópia/pickle de exceção, `Link` aceitar id e code que não se correspondem (verificar isso acoplaria o modelo ao base62 e quebraria na permutação da Fase 8 do V2), `Click` não ter identidade própria, e `encode` não checar tipo em tempo de execução.
- **Documento de aprendizado:** `docs/learning/fase-1-dominio.md`, com o exercício em `docs/learning/exercicio_fase_1.py`.

## Fase 2 — Portas, casos de uso e viewmodels

- [x] ~~`domain/service/url_hash.py`: a chave da deduplicação, que a Fase 1 registrou como sendo desta fase~~ **(item que a lista não pedia)**
  - **`hash_url(url) -> UrlHash`, `hashlib.sha256` sobre os bytes UTF-8, em hexadecimal.** O encoding é explícito e nunca o default da plataforma: a mesma URL tem que dar a mesma chave em qualquer máquina que rode isto.
  - **Mora no domínio, e isso não briga com a docstring do `Link`.** O `Link` diz que o hash é "chave de índice de tamanho fixo — assunto de persistência", e continua certo: o **índice** é persistência, mas a **regra** — duas requisições significam o mesmo link quando as strings são idênticas byte a byte — é regra de negócio, a mesma que faz o `url_policy` não normalizar nada. Com o hash no repositório, o repositório passaria a ser dono da decisão sobre quando dois links são um só.
  - **`UrlHash = NewType("UrlHash", str)`**, uma linha, custo zero em runtime. Não contradiz a decisão da Fase 1 de que `find_by_url_hash` recebe `str` e não um objeto — um `NewType` **é** um `str`, só não é *qualquer* `str` — e compra o único erro que valia prevenir: `find_by_url_hash(command.url)` passa a ser `error: Argument 1 ... has incompatible type "str"`. Sem ele, essa chamada é bem tipada, não acha nada para sempre e escreve uma segunda linha para uma URL que já tinha uma: deduplicação falhando em aberto, sem nada para notar. Um adaptador que anote `str` puro continua conformando por contravariância, então a Fase 4 não fica presa ao tipo.
  - **Verificado:** 11 testes em `tests/unit/test_url_hash.py` — um digest conhecido, a idempotência, cinco pares que um normalizador dobraria (barra final, caixa do esquema, caixa do host, ordem de query string, fragmento), o encoding UTF-8 afirmado contra o `hashlib`, e o tamanho fixo de 64 para URLs de 1 a 4020 caracteres.
  - **Fora deste item:** uma constante `HASH_LENGTH = 64`. Ninguém na Fase 2 a chamaria, e a Fase 1 fechou com esse mesmo julgamento sobre o `is_reserved_code`. A migration da Fase 4 escreve o literal.
- [x] ~~`application/port/outbound/`: `LinkRepository` (`next_id`, `find_by_code`, `find_by_url_hash`, `save`), `ClickRepository` (`record`, `count_by_link`) e `Clock` (`now`), todos como `Protocol`~~
  - **`save(self, link: Link, *, url_hash: UrlHash) -> bool`, e o `bool` é a decisão da fase.** `False` diz uma coisa só: o índice único recusou porque outra requisição chegou antes. É resultado **ordinário** de um sistema concorrente correto, então é valor de retorno e não exceção — uma exceção poria o caminho de deduplicação em todo stack trace, e um `IntegrityError` atravessando a porta arrastaria o SQLAlchemy para dentro do `application`. `Link | None` foi recusado por dois motivos: no sucesso ele só pode devolver o objeto que recebeu (as regras 1 e 9 proíbem default no banco, então a linha escrita não pode diferir da construída), e ele convida o implementador da Fase 4 a resolver a corrida dentro do repositório — o que apaga o único bit que decide `201` de `200`.
  - **`url_hash` é keyword-only, e isso foi medido.** O mypy **não** confere nome de parâmetro posicional ao casar uma classe com um `Protocol` — um fake com `find_by_url_hash(self, digest: str)` passa limpo contra uma porta com `url_hash` — e **confere** o de keyword-only, falhando com as duas assinaturas impressas. O `*` compra a garantia do nome exatamente onde um nome pode ser trocado. `/` só *proibiria* chamada por palavra-chave, que é outra coisa.
  - **Nenhum `@runtime_checkable`.** `isinstance` contra Protocol confere que os métodos existem e nada sobre assinatura ou retorno — aceitaria um `save` que devolve `str`. Uma checagem que não pode falhar pelo motivo que interessa é pior do que nenhuma.
  - **Nenhum `commit`, nenhum `flush`, nenhum `UnitOfWork`**, decidido agora e não descoberto na Fase 4: um caso de uso capaz de commitar é capaz de commitar metade de um fluxo, e o método não significaria nada em memória — viraria no-op em todo fake. A transação pertence à borda da requisição.
  - **`find_by_code` recebe `ShortCode` e `count_by_link` recebe o id.** O primeiro porque, quando a pergunta vale a pena, a string já foi provada código, então o lixo da rota catch-all nunca vira ida ao banco. O segundo porque o id é o que a chave estrangeira e o índice têm; entregar a entidade inteira para ler um inteiro sugere que a contagem é propriedade do link, que é a crença que termina numa coluna contador.
- [x] ~~`application/port/inbound/`: `CreateLinkUseCase`, `ResolveLinkUseCase` e `GetLinkDetailsUseCase`, também `Protocol`~~
  - **`create` / `resolve` / `get_details`, nunca um `execute` uniforme.** O argumento é estrutural: um `Protocol` não tem identidade nominal, então **o nome do método é a identidade do tipo**. Três portas declarando `execute` são três formas que uma classe satisfaz por acidente, e o `dependencies.py` da Fase 3 entregando o `*Impl` errado ao controller certo passaria no mypy. `__call__` é pior ainda — colapsa a porta num `Callable` e faz o `Depends` ler como se o objeto fosse o próprio provedor. A justificativa mora uma vez só, na docstring de `port/inbound/__init__.py`.
  - **`resolve` recebe o contexto do clique por parâmetros keyword-only e sem default**, e não um `ResolveLinkCommand`. `CreateLinkCommand` se justifica porque o `POST /links` tem um **corpo de requisição** para modelar — é onde o alias customizado do V2 aterrissa sem mexer em assinatura de porta; cabeçalho e endereço de socket não são isso. Keyword-only porque `user_agent` e `referer` são `str | None` vizinhos, que é a mesma armadilha que fez o `Click` ser `kw_only`. Sem default porque o único chamador é um controller com uma `Request` na mão, que sempre sabe os três — mesmo quando a resposta é `None` — e um default deixaria um clique perder contexto por omissão em vez de por fato.
  - **`code` é `str` nas duas portas de leitura**, porque a rota é catch-all na raiz e o valor frequentemente não é um código — que é justamente o caso que precisa ser respondido. `LinkNotFoundError.code` já era `str` pelo mesmo motivo.
- [x] ~~`application/viewmodel/`: `CreateLinkCommand`, `LinkResult` e `RedirectResult` como dataclasses frozen. É a fronteira de saída — o objeto de domínio não atravessa para o adaptador~~
  - **São quatro, e não três: `LinkDetailsResult` foi acrescentado.** O motivo é comportamental e não estético. Um resultado compartilhado carregando `was_created` **e** `total_clicks` obrigaria o `CreateLinkUseCaseImpl` a segurar um `ClickRepository` e a rodar um `COUNT` no caminho de **escrita**, para preencher um campo que o corpo do `POST /links` nem tem — ou a cravar um `0`, verdade para link novo e mentira no caminho de deduplicação. Três declarações de campo repetidas são o defeito mais barato. Achatado e não aninhado: aninhar economizaria três linhas e custaria um salto por `details.link.code` a cada leitura, além de impedir que o modelo de resposta da Fase 3 seja cópia campo a campo.
  - **`code` é `str`, nunca `ShortCode`** — a fronteira é aqui, e é para isso que o `ShortCode` tem `__str__`. **Não tem `short_url`**, porque montá-la exige o `BASE_URL` e a aplicação nunca adivinha o host em que responde. **Não tem `id`**, e o motivo certo é que nenhum corpo de resposta do contrato carrega um — e **não** que publicá-lo vazaria algo, já que `code` *é* `base62(id)` e o código enumerável já é custo aceito e documentado na ADR-0002.
  - **`was_created`, no passado**, para não ser lido contra o `created_at` da linha de cima. É o inteiro da decisão `201` versus `200`. Um `tuple[LinkResult, bool]` foi recusado: ele é silenciosamente atribuível a `tuple[LinkResult, int]`, porque `bool` é subtipo de `int` e tupla é covariante — exatamente o tipo de intercambialidade acidental que os verbos próprios existem para impedir.
  - **`RedirectResult` tem um campo só e ainda assim é um tipo.** Num encurtador a URL longa, a URL curta e o código são todos `str`, e só o nome os separa. Ele não carrega status: que o redirect é `302` e não `301` é decisão do adaptador web.
- [x] ~~`application/usecase/`: os três `*Impl`, recebendo as portas por construtor. A ordem dentro do `CreateLinkUseCaseImpl` é deliberada e vai documentada em subitem: validar o destino antes de qualquer efeito, calcular o hash, procurar, só então pedir o `next_id`, converter para base 62 e salvar~~
  - **A ordem, com o porquê de cada passo ser onde é:** `validate_target_url` **primeiro**, e não apenas cedo — `next_id` move a sequence mesmo dentro de transação que faz rollback, então validar depois deixaria uma URL recusada queimar um código que nada jamais responde. Depois o hash; depois o `SELECT` rápido, que devolve sem gastar id nem tentar escrita; só então `next_id`, que vem **antes** do insert e é o que permite calcular o código no domínio puro e escrever a linha com `code NOT NULL` num statement só; então o `save` com `ON CONFLICT (url_hash) DO NOTHING`; e, se ele devolveu `False`, o `SELECT` de reentrada.
  - **Quem perde a corrida responde com o vencedor, nunca com o `link` que construiu** — cujo id e código nunca foram escritos e não nomeiam nada. O valor de sequence gasto vira buraco, o que é esperado: a sequence gera ids, não conta links.
  - **A releitura que não acha nada é `RuntimeError`, e a frase é precisa.** Não é `DomainError`: a taxonomia da Fase 1 é fechada, cada membro é uma linha da tabela de Problem Details e uma regra que um chamador quebrou; aqui nenhum cliente causou e nenhum conserta, então a linha certa é a do 500. Não é `assert`, que some com `-O` justamente no galho que não pode sumir calado. E a redação é **"não deveria ser alcançável sob READ COMMITTED"**, e não "impossível": o `ON CONFLICT DO NOTHING` espera o inserter conflitante, e se ele abortou este insert prossegue, se commitou o próximo statement pega snapshot novo — e nada no V1 apaga link. Sob REPEATABLE READ a releitura mantém o snapshot original e o galho **fica** alcançável. O nível de isolamento é pré-condição do argumento e está escrito no comentário.
  - **`link_lookup.require_link` é compartilhado pelos dois casos de uso de leitura**, e o pacote ganhou um módulo que não é caso de uso — a docstring de `usecase/__init__.py` foi emendada no mesmo commit. O motivo de compartilhar é a largura do `try`, que envolve **uma expressão**: é isso que faz a decisão da Fase 1 (`DomainError` estende `Exception` e não `ValueError`) pagar, porque este `except` comprovadamente não engole um `InvalidTargetUrlError` e não responde 404 a algo que merecia 400 — e também não engole o `ValueError` que o `Link.__post_init__` levantaria numa linha corrompida. Duas cópias disso seriam duas chances de alargar por uma linha.
  - **Parsear antes de consultar não é só sobre o erro:** construir o `ShortCode` recusa `/favicon.ico`, `/robots.txt` e toda sonda de scanner **antes de qualquer chamada ao repositório**. Tem um teste com um repositório que falha se for consultado, afirmando exatamente isso.
  - **`_as_result` é função privada do módulo do caso de uso, e nunca `LinkResult.from_link`.** Um classmethod desses faria o `viewmodel` importar `domain.model` — exatamente o que ele existe para barrar — e quebraria o contrato novo do `.importlinter` na primeira linha.
  - **Uma falha ao gravar o clique derruba o redirect.** Nada envolve o `record` em `try`. Não há fila (ADR-0003) nem outbox, então engolir a falha transformaria um erro de banco no único caminho de escrita desta rota numa linha de log sem segundo alarme — e um link que para de ser medido em silêncio é exatamente o que responder `302` em vez de `301` existe para evitar. **Consequência fixada para a Fase 4:** o `ClickRepositoryImpl.record` tem que emitir o `INSERT` na hora, e não apenas `session.add`, senão a falha chega no teardown da dependência, depois da resposta já enviada, e esta decisão vira falsa na prática. Está escrito na docstring da porta.
  - **`GetLinkDetailsUseCaseImpl` recebe duas portas e nenhum `Clock`**: ele só lê, então não tem o que carimbar. Parâmetro guardado e nunca lido é uma mentira que o arquivo de fiação tem que continuar contando.
- [x] ~~`tests/fakes.py`: `InMemoryLinkRepository`, `InMemoryClickRepository` e `FixedClock` implementando as Protocols. **Fakes, não mocks** — um mock aqui faria o teste afirmar que uma chamada aconteceu em vez de afirmar o resultado~~
  - **A conformidade virou prova de máquina, e isso exigiu três edições num commit só.** O critério da fase diz que os fakes implementam as Protocols, e antes disto **nada checava**: `files = ["src"]` no `pyproject.toml` e `uv run mypy src` no CI. Agora `files = ["src", "tests"]`, mais `mypy_path = ["src", "."]` e `explicit_package_bases = true` — que não são polimento: `tests/` não tem `__init__.py` porque a Fase 0 escolheu `--import-mode=importlib` para não precisar de um, e sem essas duas linhas o mypy mapeia o mesmo arquivo para dois nomes de módulo e para antes de checar qualquer coisa. **E o `ci.yml` teve que mudar junto**, porque caminho explícito **sobrepõe** `files`: `uv run mypy src` vê 48 arquivos e `uv run mypy` vê 64. Sem essa terceira edição, a prova nunca rodaria no gate que bloqueia o merge. A linha da tabela de comandos do `CLAUDE.md` mudou junto.
  - **A prova é um bloco `if TYPE_CHECKING:` atribuindo *instâncias* a nomes anotados com a porta.** Nunca roda, então não custa import, não custa cobertura e não vira `F841`. Instâncias e não `type[Port] = Fake` porque a forma com instância imprime o membro conflitante e as duas assinaturas.
  - **`insert_before_next_save(rival, *, url_hash)` é como a corrida é armada** — commita um rival dentro da janela, sem thread, sem sleep, sem flake, e vale uma vez só porque uma corrida é um momento e não uma condição. Repare no que ele **não** é: não é uma flag "devolva `False` desta vez". Uma flag só testa a flag; aqui o `False` cai da regra de unicidade do próprio store, no mesmo momento em que o índice único produziria.
  - **`issued_ids` é estado, não gravação de chamada:** `nextval` move a sequence com ou sem linha depois, então os ids entregues são estado observável do banco, e a lista vazia é como um teste prova que uma URL recusada nunca chegou ao gerador.
  - **O índice único em `code` deliberadamente não é modelado.** `next_id` é monotônico e `ShortCode.from_id` é bijeção, então colisão de código é inalcançável no fake — um galho para ela seria código morto que o gate de cobertura da Fase 5 teria que ser ensinado a ignorar, e levantar `ValueError` ali cairia na mesma família de exceção que os casos de uso capturam para produzir 404, enquanto o repositório real levantaria `IntegrityError`. Os dois discordariam no dia em que a Fase 5 os unificar numa suíte de contrato.
  - **O caso impossível não mora no fake.** `winner is None` exige um store que se contradiz, então ele é uma subclasse de três linhas dentro de `test_create_link_use_case.py` — os fakes continuam um modelo do banco em vez de um museu de estados impossíveis.
  - **`tests/unit/test_fakes.py` existe** porque os fakes carregam lógica de verdade e toda asserção de caso de uso repousa nela, enquanto o mypy prova forma e explicitamente não comportamento. Na Fase 5 esse arquivo é a semente da suíte de contrato parametrizada sobre o fake e o repositório real.
- [x] ~~Testes unitários dos três casos de uso contra os fakes, cobrindo também o caminho de deduplicação~~
  - **68 testes novos, 324 no total, todos sem Docker e sem banco, em 0,7 s.** Ciclo teste vermelho, implementação, verde, e só então o commit com os dois juntos — nunca um commit só de teste, porque a `main` é mergeada por rebase e um commit intermediário vermelho entraria no histórico em silêncio.
  - **Cada suíte constrói o sujeito por um helper anotado com a porta de entrada** (`def _use_case(...) -> CreateLinkUseCase`). A anotação **é** a asserção: ela prova que o `*Impl` satisfaz a Protocol de entrada, o que nada mais faz antes da Fase 3, e faz o nome do `*Impl` aparecer uma vez só por arquivo.
  - **A suíte foi medida por mutação, não por leitura, e quatro mutantes sobreviveram à primeira rodada:** `hash_url(command.url.lower())` (toda constante de URL da suíte já era minúscula), `url=command.url.rstrip("/")` (nenhuma URL de teste terminava em barra), `link_id=1` no clique (todo link exercitado era o primeiro, então o id era sempre 1) e `occurred_at=link.created_at` (uma constante `INSTANT` só alimentava o relógio **e** o link). Nenhum é bug no código; os quatro são buracos de teste. As correções: URLs que diferem só em caixa e barra final, exercitar o **segundo** link e não o primeiro, e dois instantes distintos (`CREATED_AT` e `VISITED_AT`). Rodada final: **11 mutantes, 10 mortos**.
  - **Caveats — um mutante equivalente, registrado em vez de escondido:** trocar `except ValueError` por `except Exception` no `require_link` não muda nada observável, porque o `try` tem uma expressão só e `ShortCode(code)` não levanta outra coisa sob a assinatura `code: str`. O mutante que importa — alargar o `try` para cobrir o `find_by_code` — é morto pelo teste do repositório com linha corrompida.
  - **Verificado:** `uv run pytest` → `324 passed`. Cobertura de `application/` e `domain/` em **100%** de linha e de galho; o único trecho descoberto do projeto é `settings.py:34`, herdado da Fase 0.
- [x] ~~Um quarto contrato no `.importlinter`, com ADR~~ **(item que a lista não pedia)**
  - **`boundary-carries-no-domain-object`, cobrindo `application.viewmodel` e `application.port.inbound`.** O contrato `layers` permite `adapter -> domain`, e permite corretamente, porque o mapper da Fase 4 precisa importar `Link`. Consequência: nada impedia um `LinkResult` de ter `code: ShortCode` nem uma porta de entrada de declarar `-> Link`, e as duas coisas passavam no mypy, no ruff e nos três contratos existentes.
  - **As duas metades, e não só a primeira.** Cobrir só o `viewmodel` protegeria a metade que ninguém ia quebrar; a metade perigosa é a assinatura que o controller lê.
  - **Verificado nos dois sentidos:** com `ShortCode` importado em `link_result.py`, quebra **duas vezes** (direto pela metade `viewmodel`, e pela cadeia `port.inbound.create_link_use_case -> link_result -> short_code`); com `Link` importado em `application/port/inbound/get_link_details_use_case.py`, quebra só pela metade `port.inbound`. `docs/adr/0004-fronteira-sem-objeto-de-dominio.md` registra a decisão, porque o `CLAUDE.md` exige ADR para mexer no `.importlinter` — e a lista de contratos do próprio `CLAUDE.md` ganhou a quarta entrada.
  - **Caveat operacional:** o `lint-imports` tem cache em `.import_linter_cache/`. Ao conferir que um contrato quebra de propósito, apague o cache antes, ou o resultado velho aparece como quebra que não sai mais.
- **Critério:** ~~os três casos de uso verdes sem nada de infraestrutura de pé, e `lint-imports` provando que `application` não importa `fastapi`, `sqlalchemy` nem `pydantic`.~~ **Atendido.** `uv run pytest` → `324 passed` (0,7 s, sem Docker, sem banco, sem rede); `uv run mypy` → `Success: no issues found in 64 source files`; `uv run ruff check .` e `ruff format --check .` limpos; `uv run lint-imports` → **`Contracts: 4 kept, 0 broken.`** `git diff --stat main..HEAD`: **32 arquivos, 1.807 inserções** — 565 em `src/` e 1.059 em `tests/`, e o resto em documentação; quase o dobro de teste sobre código é a proporção que esta fase deveria ter.
  - **Não-vacuidade da prova de conformidade, medida e não afirmada:** renomear `url_hash` para `digest` no `InMemoryLinkRepository` faz o `uv run mypy` reprovar em `tests/fakes.py:147` com `Following member(s) of "InMemoryLinkRepository" have conflicts`, imprimindo as duas assinaturas. Uma checagem que ninguém viu falhar é uma checagem que ninguém sabe se funciona.
  - **Os catorze commits foram rodados um a um**, cada um em seu próprio worktree, contra `pytest`, `mypy`, `ruff check`, `ruff format --check` e `lint-imports`: **14 verdes, 0 vermelhos.**
  - **Revisão:** o diff passou por revisão adversarial em cinco lentes (correção e concorrência, aderência ao spec, qualidade de teste por mutação, tipagem e pureza do hexágono, e sobrevivência às Fases 3 e 4), com dois céticos independentes por achado. Cinco achados sobreviveram: os buracos de teste acima, a lista de contratos desatualizada no `CLAUDE.md`, e dois erros na própria ADR-0004 — ela dizia "quinto contrato" onde é o quarto (a `main` tem três, e a seção `[importlinter]` de configuração tinha sido contada como contrato), e citava um nome de arquivo que existe em dois pacotes, de modo que quem reproduzisse a verificação no arquivo errado veria o contrato verde e concluiria que a ADR mente. Os cinco foram corrigidos. **Nenhum achado atingiu o código de produção.**
- **Documento de aprendizado:** `docs/learning/fase-2-portas-e-use-cases.md`, com o exercício em `docs/learning/exercicio_fase_2.py` — o `save` do fake e a dança de quatro passos, com os testes já escritos.

## Fase 3 — Adaptador web, ainda em memória

- [x] ~~`adapter/web/dto/request/` e `dto/response/`: modelos Pydantic v2. São a fronteira HTTP e não entram para dentro; quem cruza a camada é o `viewmodel`~~
  - **`CreateLinkRequest.url` é `str`, nunca `HttpUrl` — é a decisão da fase, e virou a ADR-0005.** O Pydantic normaliza (`https://example.com` → `https://example.com/`, `Example.com` → `example.com`), o que quebraria a regra 5 e a chave `sha256` das regras 6 e 7 de uma vez; e ele decide política pior do que a `url_policy`, transformando `javascript:` em 422 quando o contrato manda 400, ao mesmo tempo em que **aceita** `http://localhost:8000/admin` e `http://169.254.169.254/`. Sem `max_length` pelo mesmo motivo: o limite de 2048 mora na política, e repetido aqui seriam dois limites livres para divergir. `extra="forbid"` para campo digitado errado virar 422 em vez de silêncio.
  - **Quatro modelos de resposta e um `public_url.py`.** `LinkResponse`, `LinkDetailsResponse`, `HealthResponse` e `ProblemResponse` (com `FieldError`). Cada um com `from_result(result, *, base_url)` — classmethod, e aqui é legal, porque `adapter → application` é a direção permitida; o inverso é o que a ADR-0004 proíbe. `short_url` é o único campo sem contrapartida no viewmodel e não pode ter uma: montá-la exige o `BASE_URL`. `public_url.py` é o único lugar que junta origem e caminho, e ele faz `rstrip("/")` — barra final numa variável de ambiente é o erro de operação mais comum que existe, e produziria `https://sho.rt//abcdefg` sem reclamar.
  - **`was_created` não aparece em corpo nenhum**, de propósito: quem carrega esse fato é o status. Tem teste afirmando que os dois resultados renderizam idêntico.
  - **Verificado:** 18 testes em `tests/unit/test_create_link_request.py`, 8 em `test_web_responses.py`, 5 em `test_public_url.py`. **Não-vacuidade medida:** trocando `url: str` por `url: AnyHttpUrl`, **11 testes reprovam** — os quatro de normalização, os seis de política e o que afirma que o valor que chega é a própria string.
- [x] ~~`adapter/web/link_controller.py` (`POST /links` e `GET /links/{code}`), `health_controller.py` (`GET /health`) e `redirect_controller.py` (`GET /{code}`)~~
  - **Todo endpoint é `def`, nenhum `async def`**, e nenhum teste é `async`. Não entrou `pytest-asyncio`.
  - **Os controllers importam só os aliases `Annotated[Porta, Depends(...)]`** de `dependencies.py`. Nenhum deles menciona um `...Impl`.
  - **`/health` não declara dependência nenhuma**, e tem um teste lendo `route.dependant.dependencies == []` para travar isso. Um endpoint de saúde que precisasse de sessão ou de setting poderia falhar por motivo que nada tem a ver com a saúde que ele reporta. Ele responde `{"status": "ok"}` estático — a Fase 4 é quem lhe dá o `SELECT 1` e o 503.
  - **Verificado:** `tests/unit/test_create_link_endpoint.py` (21), `test_link_details_endpoint.py` (12), `test_redirect_endpoint.py` (16), `test_health_endpoint.py` (3).
- [x] ~~`POST /links` devolve `201` com header `Location` quando cria, e `200` quando a URL já existia. O chamador distingue os dois casos~~
  - **O `status_code=201` do decorator é o default, e o `Response` injetado é como o 200 é expresso** sem abrir mão do `response_model` nem do schema gerado. O bit que decide é o `LinkResult.was_created` da Fase 2 — o controller só traduz.
  - **`Location` aponta para `{base_url}/links/{code}`, não para a URL curta.** O recurso criado é o membro da coleção `/links`; `GET /{code}` não é uma representação dele, é uma **ação** sobre ele. E a URL curta já está no corpo, então um header repetindo-a não acrescentaria informação. Tem teste que pega o `Location`, requisita o caminho e afirma que ele devolve os detalhes do link recém-criado.
  - **Nada de `Location` no 200**, porque nada foi criado para ele apontar.
  - **Verificado:** `POST` da mesma URL duas vezes devolve `201` e depois `200` com o mesmo corpo, e `len(links.rows) == 1`. Quatro pares de URLs que um `HttpUrl` teria unificado (barra final, caixa, ordem de query, fragmento) produzem **dois** links cada.
- [x] ~~`GET /{code}` com `RedirectResponse(url, status_code=302)`. O default do Starlette é `307`, que preserva o método e não é o que um link curto significa~~
  - **`status_code=HTTPStatus.FOUND` explícito**, mais `Cache-Control: no-store`. O header é acréscimo consciente ao que a fase pediu: um `302` já não é cacheável sem informação explícita de frescor, então ele não muda regra nenhuma — torna a ADR-0001 verificável com um `curl -i` em vez de com uma leitura da RFC 9111.
  - **O contexto do clique sai da `Request`, e o endereço é parseado no adaptador.** Três resultados ordinários: sem cliente, não parseia, parseia. Os três têm teste — o do `scope` sem cliente chama `_client_address` direto, porque o `TestClient` sempre reporta um par.
  - **`X-Forwarded-For` não é lido aqui, e a frase precisa do qualificador.** O uvicorn roda com `proxy_headers=True` e `forwarded_allow_ips` de `127.0.0.1` por padrão, então o `ProxyHeadersMiddleware` dele reescreve `scope["client"]` a partir do header para quem chega por loopback. Medido: através do middleware, um chamador loopback mandando `X-Forwarded-For: 203.0.113.9` grava `203.0.113.9`; a aplicação nua grava `127.0.0.1`. Isso é divisão de trabalho e não buraco — quais saltos merecem crédito é fato de implantação, e o servidor é onde a lista de proxies confiáveis se configura. **Consequência operacional: rodar com `--forwarded-allow-ips=""` enquanto não houver proxy.** Tem teste dirigindo a aplicação através desse middleware, então o parágrafo é conferido e não acreditado.
  - **Falha ao gravar o clique derruba o redirect**, e nada envolve o `record` em `try` — a decisão da Fase 2, mantida.
- [x] ~~`adapter/web/handler/`: o enum `ProblemType` e os handlers RFC 7807, **substituindo** também o handler default de `RequestValidationError` para que todo erro da API saia no mesmo envelope `application/problem+json`~~ **(com um quinto membro que a lista não pedia, e uma ADR)**
  - **`ProblemType` é um `StrEnum` de slugs, e o status não mora nele** — mesma forma do `RejectionReason`. O pareamento erro→status fica nos handlers, que é o que a docstring do pacote já dizia desde a Fase 0.
  - **Cinco handlers, e o quinto é o que faz a afirmação da fase ser verdadeira em vez de quase.** As quatro linhas da tabela cobrem o que o *nosso* código levanta; o roteador levanta `405` e `404` antes de qualquer controller rodar, e sem handler para `HTTPException` os dois saem como `{"detail": ...}` em `application/json`. O membro novo é `http-error`, e o handler repassa `exc.headers` — o `Allow` que um `405` é inútil sem. **ADR-0006**, e a tabela do `CLAUDE.md` ganhou a quinta linha.
  - **A lista de campos do 422 é reconstruída, nunca repassada.** `exc.errors()` traz `input` (o payload do próprio chamador) e `ctx` (valores que nem sempre sobrevivem a JSON). Tem teste mandando `{"url": {"secret": "do-not-echo-me"}}` e afirmando que a string não aparece na resposta.
  - **Caveat que só um teste encontrou:** `logger.exception` no handler do 500 escreve `NoneType: None` no lugar do traceback. O Starlette roda handler síncrono via `run_in_threadpool`, então ele executa em outra thread que a do `except`, e `sys.exc_info()` — que é estado por thread — vem vazio. O handler passa `exc_info=exc` explicitamente, e há um teste afirmando que o traceback está no log. Um 500 com corpo genérico **e** log vazio é uma falha que ninguém fica sabendo.
  - **Caveat de tipagem:** `add_exception_handler` recebe `Callable[[Request, Exception], Response]`, e por contravariância uma assinatura precisa não é atribuível a isso. Quatro das cinco linhas de registro carregam `# type: ignore[arg-type]`; a quinta não precisa, porque `_handle_unexpected` já recebe `Exception`. Removendo os quatro, o `uv run mypy` acusa **quatro** erros, nessas quatro linhas e em nenhuma outra.
  - **Verificado:** 12 testes em `tests/unit/test_problem_details.py`, contra uma aplicação nua que só levanta — o mapeamento é testado sem depender de ordem de rota, de fiação nem de caso de uso.
- [x] ~~`adapter/config/dependencies.py`: a fiação por `Depends`, num lugar só. Os controllers dependem das portas `inbound`, nunca dos `*Impl`~~
  - **Todo provider é anotado com a porta, nunca com a classe**, e a anotação **é** a asserção: é ela que faz o mypy provar que `InMemoryLinkRepository` satisfaz `LinkRepository` e que `CreateLinkUseCaseImpl` satisfaz `CreateLinkUseCase`. Este arquivo é a prova de conformidade estrutural do código de produção, como o bloco `TYPE_CHECKING` de `tests/fakes.py` é a dos dublês.
  - **`@lru_cache` nas três portas de saída e em nenhum caso de uso.** Um store em memória reconstruído por requisição seria um banco vazio a cada requisição; um caso de uso é um objeto de três linhas segurando referências, e cachear só esconderia quais colaboradores ele recebeu.
  - **Aliases `Annotated[Porta, Depends(...)]`**, e não a forma antiga com valor default, que põe um default onde não há default e atrapalha o checador.
  - **Verificado:** 4 testes em `tests/unit/test_dependencies.py`, incluindo um que cria um link pelo caso de uso fiado, segue o código pelo outro e lê o total pelo terceiro — a asserção de que os três receberam o mesmo repositório.
- [x] ~~`adapter/config/clock.py` com `SystemClock`~~
  - **`datetime.now(UTC)`, nunca `datetime.now()`.** É uma classe e não uma função porque a porta é um `Protocol` com um método `now`, e porque a fiação entrega um objeto anotado com a porta — que é o que faz o mypy conferir a conformidade.
  - **Verificado:** 4 testes, incluindo o que prende o instante entre duas leituras do relógio de parede.
- [x] ~~`main.py`: registra os exception handlers, depois os routers, com o `redirect_controller` **por último** — o catch-all na raiz engole `/links`, `/health` e `/docs` se vier antes~~
  - **`create_app()` mais `app = create_app()`.** O `uvicorn url_shortener.main:app` precisa do objeto no import; um teste precisa de um app novo, porque `dependency_overrides` é estado no objeto. Tem teste importando o `app` de módulo, para o nome de que o comando de execução depende não poder ser apagado em silêncio.
  - **Correção ao enunciado do item: o `/docs` não corre risco.** `FastAPI.__init__` registra `/docs`, `/redoc` e `/openapi.json` antes de qualquer `include_router` nosso, então nenhuma ordem que a gente escolha os alcança. Quem o catch-all engole são `/links` e `/health`. Subindo o `redirect_controller` para o topo, **4 testes reprovam**, incluindo o `/health` respondendo 404.
  - **`redirect_slashes=False`**, acréscimo à lista. O 307 de conveniência do Starlette monta o `Location` a partir do `Host` da requisição — exatamente o palpite que o `BASE_URL` existe para recusar, já que atrás de um proxy esse header nomeia algo que o serviço nunca soube que atende. Nenhuma das quatro rotas é definida com barra final, então não se perde nada; `/links/` e `/0000001/` viram 404 no envelope da própria API.
  - **`_describe_errors_accurately` conserta o documento OpenAPI uma vez e o guarda em `openapi_schema`.** Duas correções, e as duas existem porque este projeto chama o `/docs` de interface: o FastAPI arquiva todo corpo de problema sob `application/json`, e injeta um `422` apontando para o `HTTPValidationError` dele em toda operação com parâmetro — inclusive nas duas rotas `{code}`, onde esse 422 é **impossível** (`code` é `str`, e todo segmento de caminho é um) e onde a forma anunciada é o `{"detail": [...]}` que esta fase substituiu. Declarar `content` à mão não resolve: o FastAPI mescla a entrada dele ao lado e ficam duas, uma errada.
  - **`main.py` fica fora do contrato `layers` do import-linter**, que nomeia `adapter`, `application` e `domain`. É o composition root, e incluí-lo exigiria mexer no `.importlinter` — o que este projeto cobra ADR — para restringir vinte linhas que existem para importar coisas. Decisão, não esquecimento.
  - **Verificado:** 10 testes em `tests/unit/test_app_routes.py`, incluindo os que leem o documento gerado. Sem `_describe_errors_accurately` e sem `redirect_slashes=False`, **4 testes reprovam**.
- [x] ~~Testes de API com `fastapi.testclient.TestClient`, sobrescrevendo as dependências pelos fakes da Fase 2. Rápidos, sem Docker, e ficam no conjunto de testes unitários~~
  - **124 testes novos, 448 no total, 2,4 s, sem Docker, sem banco e sem rede.** Ciclo teste vermelho, implementação, verde, e o commit com os dois juntos.
  - **O que se sobrescreve são as portas de saída, nunca os casos de uso.** O FastAPI resolve override através de sub-dependência, então trocar a folha basta — e cada teste de API roda o `CreateLinkUseCaseImpl` de verdade, o fluxo de deduplicação de verdade e a fiação de verdade. Sobrescrever o caso de uso testaria o controller contra um dublê e não provaria nada sobre o sistema.
  - **`follow_redirects=False` na fixture não é preferência:** seguir o `302` faria o cliente de teste emitir uma requisição real para o destino, e um teste unitário passaria a medir o site de outra pessoa.
  - **`client=("203.0.113.7", 51234)` na fixture** porque o default do `TestClient` é a string `testclient`, que não parseia como endereço — sem isso, todo clique da suíte gravaria `None` e o parse nunca seria exercitado.
  - **Caveat:** o Starlette 1.6 mudou o `include_router`. As rotas do router incluído **não** são mais achatadas em `app.routes`; entra uma entrada opaca (`_IncludedRouter`) por router, com o router original em `original_router`. Um teste que procurasse `/health` em `app.routes` não acha nada.
- [x] ~~`adapter/persistence/in_memory_repositories.py`, para a fase ter o que rodar~~ **(item que a lista não pedia)**
  - **Existe porque o critério exige o `uvicorn` de pé, e os fakes moram em `tests/`, que o `src/` não pode importar.** Arquivo único, com data de validade na docstring: a Fase 4 o apaga e troca dois providers. Não é o fake de teste e não deve virar um — o fake tem `insert_before_next_save`, `issued_ids` e `rows`, aforândancias que produção não deve expor.
  - **Tem `threading.Lock`, e nenhum teste finge prová-lo.** O FastAPI roda endpoint `def` em threadpool, então duas requisições tocam mesmo esses dicionários ao mesmo tempo. Mas **foi medido**: sob o GIL do CPython 3.13 o `+= 1` do `next_id` nunca interleava — 32 threads, 20 000 incrementos cada, `sys.setswitchinterval(1e-9)`, **zero ids duplicados** em execuções repetidas, porque o eval breaker não é checado entre o `LOAD_ATTR` e o `STORE_ATTR`. O lock fica (atomicidade do GIL é detalhe de implementação e é falsa num build free-threaded), mas uma checagem que não pode reprovar pelo motivo que interessa é pior do que nenhuma. **Mutante equivalente registrado, na mesma linha do que a Fase 2 fez.**
  - **O teste de concorrência que existe afirma o que consegue afirmar:** que nada trava e que a loja fica coerente. Ele reprova de verdade — verificado injetando um `save` que toma o mesmo lock duas vezes: as oito threads ficam vivas e a asserção de `join` com prazo pega.
- **Critério:** ~~`uv run uvicorn url_shortener.main:app --reload` sobe; `/docs` lista as quatro rotas **e continua acessível**; `GET /{code}` de um código conhecido devolve `302` com `Location`; um corpo inválido devolve `application/problem+json`.~~ **Atendido.** `uv run pytest` → **`448 passed`** (2,4 s); `uv run mypy` → `Success: no issues found in 92 source files`; `uv run ruff check .` e `ruff format --check .` limpos; `uv run lint-imports` → `Analyzed 80 files, 138 dependencies. Contracts: 4 kept, 0 broken.` `git diff --stat main..HEAD`: **33 arquivos, 3.062 inserções** — 997 em `src/` e 1.799 em `tests/`.
  - **O passeio manual, contra `uvicorn ... --port 8112 --forwarded-allow-ips=""`:**
    1. `POST /links` novo → **201**, `location: http://localhost:8000/links/0000001`, corpo `{"code":"0000001","short_url":"http://localhost:8000/0000001","url":"https://docs.python.org/3/library/dataclasses.html","created_at":"2026-08-31T17:59:02.011434Z"}`
    2. mesmo `POST` de novo → **200**, **sem** `location`
    3. `GET /0000001` → **302**, `location: https://docs.python.org/3/library/dataclasses.html`, `cache-control: no-store`
    4. `GET /links/0000001` depois de dois acessos → `{"...","total_clicks":2}`
    5. `POST` com `javascript:alert(1)` → **400** `application/problem+json`, `{"type":"invalid-target-url",...,"reason":"unsupported-scheme"}`
    6. `POST {"alias":"x"}` → **422**, `{"type":"validation-error",...,"errors":[{"field":"body.url",...},{"field":"body.alias","message":"Extra inputs are not permitted",...}]}`
    7. `GET /zzzzzzz` → **404** `{"type":"link-not-found",...,"instance":"/zzzzzzz"}`
    8. `POST /health` → **405**, `allow: GET`, `{"type":"http-error","title":"Method Not Allowed",...}`
    9. `GET /0000001/` → **404** `{"type":"http-error","title":"Not Found",...}` — sem 307 e sem `Location`
    10. `/docs`, `/redoc`, `/openapi.json`, `/health` → **200** nos quatro
    11. `grep -c HTTPValidationError` sobre o `/openapi.json` → **0**
  - **A URL curta sai como `http://localhost:8000/0000001` enquanto o servidor está na 8112, e isso é o resultado certo:** `BASE_URL` é a origem pública, não o endereço de bind. É a demonstração mais barata de que a API não adivinha o host em que responde.
  - **Os dezenove commits foram rodados um a um, cada um em seu próprio worktree**, contra `pytest`, `mypy`, `ruff check`, `ruff format --check` e `lint-imports`. **19 verdes, 0 vermelhos.**
  - **Revisão:** o diff passou por revisão adversarial em cinco lentes (correção e semântica HTTP, aderência ao spec, qualidade de teste por mutação, tipagem e pureza do hexágono, e segurança da camada web), com dois céticos independentes por achado — 22 achados, 5 verificados, 1 sobrevivente, e os 17 restantes triados à mão. **Nenhum achado atingiu o comportamento das quatro rotas.** O que foi corrigido: três buracos de teste (um header ausente afirmado como string vazia, o galho de `scope` sem cliente, e três handlers com asserção parcial de corpo), duas mentiras do documento OpenAPI, o 307 de barra final, e quatro frases falsas — a do `X-Forwarded-For`, a do "cada um com um `type: ignore`", a de que a Fase 4 não tocaria em `adapter/web/`, e a de que o catch-all engoliria o `/docs`.
  - **Caveats:**
    - **`HEAD` responde `405` nas quatro rotas.** O FastAPI não acrescenta `HEAD` automático numa rota `@get` (o Starlette acrescenta nas dele, e é por isso que `HEAD /docs` responde 200). Verificadores de link e desdobradores de preview usam `HEAD`, então na prática eles leem um link curto como quebrado. **Não foi corrigido de propósito, e a medição é a razão:** acrescentar `methods=["GET","HEAD"]` no redirect faz `total_clicks` subir a cada `HEAD`, entregando a qualquer robô o poder de inflar a contagem que a regra 8 e a ADR-0001 existem para proteger; e fazer o `HEAD` não contar quebra a semântica de "`GET` sem corpo". É decisão de produto, não de rota — vai para o V2.
    - **`GET /links` responde `404 link-not-found`**, porque o catch-all casa `links` como código antes de o `405` do `POST /links` ser considerado. Nenhum `GET /links` existe no contrato; o `type` é que fica esquisito.
    - **O corpo da requisição não tem limite de tamanho.** O FastAPI lê e desserializa o corpo inteiro antes de qualquer validador, então o corte de 2048 caracteres da política só roda depois de a string existir na memória. É assunto de proxy reverso ou de limite de corpo do servidor, e está registrado como consequência na ADR-0005.
    - **`Settings` só é construído na primeira requisição**, porque `get_settings` é um `Depends`. Com `BASE_URL` ou `DATABASE_URL` faltando, o processo sobe limpo, o `/health` responde `ok` e o primeiro `POST /links` dá 500 — o que contradiz a docstring da Fase 0 ("fail loudly at startup"). Não foi corrigido aqui porque validar dentro do `create_app()` obrigaria toda a suíte a ter variáveis de ambiente; está anotado como item da Fase 4, junto do `/health` que passa a checar dependência de verdade.
- **Documento de aprendizado:** `docs/learning/fase-3-adapter-web.md`, com o exercício em `docs/learning/exercicio_fase_3.py` — a conversão dos erros do Pydantic no formato de três campos, com os testes já escritos.

## Fase 4 — Persistência em PostgreSQL

- [x] ~~`compose.yml` com o serviço `postgres`, imagem pinada por versão (nunca `latest`) e volume nomeado~~
  - **Feito.** `compose.yml` com **só** o serviço `postgres` — o `api` e o Dockerfile são da Fase 6, e um compose que nomeia serviço inexistente mente sobre o que `docker compose up` faz. Imagem `postgres:18.6-alpine`, pinada até o patch; volume nomeado `postgres-data`; `healthcheck` com `pg_isready`; credenciais idênticas às do `.env.example`, caractere por caractere.
  - **Armadilha que só apareceria meses depois:** o PostgreSQL 18 tornou o `PGDATA` da imagem oficial específico por versão (`/var/lib/postgresql/18/docker`), então o volume monta em `/var/lib/postgresql` e **não** em `/var/lib/postgresql/data`. O caminho antigo monta sem reclamar e não persiste nada.
  - **Verificado:** `docker compose up -d postgres` → `healthy` na 11ª checagem; os dados sobreviveram a um `stop`/`start` no meio da verificação do `/health`.
- [x] ~~Alembic inicializado em `migrations/`, com a primeira migration criando `link` (`id BIGSERIAL`, `code TEXT UNIQUE NOT NULL`, `url TEXT NOT NULL`, `url_hash CHAR(64) UNIQUE NOT NULL`, `created_at TIMESTAMPTZ NOT NULL`) e `click` (`id`, `link_id` com chave estrangeira, `occurred_at`, `user_agent`, `referer`, `ip INET`), mais o índice em `click(link_id)`~~
  - **Feito.** `alembic.ini` na raiz (o `CLAUDE.md` nomeia esse arquivo, não o `[tool.alembic]` do 1.16+), `migrations/env.py` lendo o DSN de `Settings` e honrando um `sqlalchemy.url` injetado — que é o gancho de que a Fase 5 precisa —, e uma revisão, `cee192947f04`, com as duas tabelas e o índice `ix_click_link_id`.
  - **O `alembic init` reprova no lint deste repositório**, e isso não estava previsto em lugar nenhum: o `env.py` gerado viola `I001` e o `context.configure` sai sobre-quebrado. O CI roda `ruff check .` na árvore inteira, então o scaffold ficaria vermelho no primeiro commit. Corrigido nos templates **e** com `[post_write_hooks]` rodando `ruff format` e `ruff check --fix` em toda revisão futura.
  - **`migrations/` entrou no `files` do mypy.** Era o único Python do repositório que decide o que o schema *é*, e o único que nada checava.
  - **Verificado:** `uv run alembic upgrade head` aplicou contra o Postgres real, e **`uv run alembic check` respondeu `No new upgrade operations detected.`** — a migration escrita à mão bate com os modelos, conferido pela máquina. O `psql \d link` confirmou `nextval('link_id_seq'::regclass)`, que era até então só uma afirmação num comentário.
  - **Caveats:** a revisão foi escrita à mão, não gerada. Sem Docker o `--autogenerate` não roda (ele compara contra um banco vivo), então o conteúdo foi conferido comparando a SQL de `alembic upgrade head --sql` com a DDL compilada de `Base.metadata` — idênticas — e depois pelo `alembic check` acima.
- [x] ~~`adapter/persistence/entity/` com os modelos SQLAlchemy 2.0 (`Mapped` e `mapped_column`), `mapper/` com a conversão entidade-domínio nos dois sentidos, e `database/session.py` com engine e sessão~~
  - **Feito.** `entity/base.py` (`DeclarativeBase` com `naming_convention`), `entity/link_entity.py`, `entity/click_entity.py`; `mapper/link_mapper.py` e `mapper/click_mapper.py`; `database/session.py` com `create_database_engine`, `create_probe_engine` e `create_session_factory`.
  - **`ip` é `Mapped[IPv4Address | IPv6Address | None]` e o mapper não converte nada.** O palpite registrado no item da Fase 2 (linha 131, agora corrigido) estava errado: o psycopg 3 registra loaders de `ipaddress` por padrão, então `ip_address(row.ip)` levantaria `ValueError` na primeira leitura.
  - **`LINK_ID_SEQUENCE` fica desanexada da metadata.** Anexá-la torna a coluna um `BIGINT` liso, sem default — e só com um `server_default` explícito vira `BIGINT DEFAULT nextval(...)`. Em qualquer dos dois casos a sequence deixa de ser *owned* pela coluna.
  - **Verificado:** 13 testes de mapper sem banco, e a prova de não-vacuidade que importa — a primeira versão deles **passava** com `ShortCode(entity.code.upper())`, porque todo código de teste era `0000007`, só dígitos. Trocado por `hBxM5A3` (de `encode(999_999_999_999)`), `.upper()` e `.lower()` passaram a reprovar 3 testes cada.
- [x] ~~`link_repository_impl.py`: `next_id` lendo `nextval` da sequence **antes** do insert — é isso que permite calcular o código no domínio puro e inserir com `code NOT NULL` num único statement — e o insert com `ON CONFLICT (url_hash) DO NOTHING`~~
  - **Feito.** `NEXT_ID_STATEMENT` compila para `SELECT nextval('link_id_seq')`, e `insert_link_statement` para `INSERT INTO link (...) VALUES (...) ON CONFLICT (url_hash) DO NOTHING RETURNING link.id`.
  - **`RETURNING`, e nunca `rowcount`.** Num `INSERT` sem `RETURNING` o SQLAlchemy não memoiza o `rowcount` e o psycopg o zera para `-1` ao fechar o cursor: `save` reportaria ter inserido o que não inseriu.
  - **Verificado:** 5 testes de SQL compilado, contra o dialeto da própria engine do projeto. Mutados: remover o alvo do `ON CONFLICT` → 2 reprovas; remover o `RETURNING` → 1 reprova.
- [x] ~~O fluxo de deduplicação em quatro passos, com o `SELECT` de reentrada no passo 4 para o caso de perder a corrida. **Nunca** check-then-insert: entre o `SELECT` que não achou e o `INSERT`, outra requisição insere~~
  - **Feito.** O fluxo já existia na Fase 2; esta fase só lhe deu o banco. `save` devolve `bool`, a corrida se resolve no caso de uso, e nenhuma `IntegrityError` atravessa a porta.
  - **Verificado contra concorrência real — o extra que o roadmap chama de mais valioso:** 24 `POST /links` simultâneos com a mesma URL → **um `201`, vinte e três `200`, um único código, uma única linha em `link`**. E `link_id_seq.last_value = 5` com 3 links: dois pedidos passaram do `SELECT`, gastaram um `nextval` e perderam no `ON CONFLICT`, voltando pelo passo 4. O buraco na sequence é a evidência de que a corrida aconteceu de fato.
  - **Caveats:** medido contra o banco local, não em CI. O teste automatizado de concorrência é da Fase 5.
- [x] ~~`click_repository_impl.py`: `INSERT` append-only e `count_by_link` por `COUNT`. Nenhum contador dentro de `link`~~
  - **Feito.** `record` emite `session.execute(insert(...))` — nunca `session.add` —, e `count_statement` compila para `SELECT count(*) FROM click WHERE click.link_id = ...`.
  - **Verificado:** o `information_schema` confirma **zero** colunas com nome de contador em `link`; 3 testes de SQL compilado, mutados (dedupe de clique → 1 reprova; contar a partir de `link` → 1 reprova). A primeira tentativa de mutação foi um `sed` que não casou e reportou 0 falhas — repetida com a substituição verificada, o teste pega. Uma mutação que não se confirmou aplicada não prova nada.
- [x] ~~Trocar as implementações em memória pelas reais em `dependencies.py`, e apagar o adaptador temporário que a Fase 3 deixou~~
  - **Feito — e este é o número da fase.** O commit da troca (`d4633c2`) tem `2 files changed, 175 insertions(+), 55 deletions(-)`: `adapter/config/dependencies.py` e `tests/unit/test_dependencies.py`. **Zero arquivos sob `adapter/web/`.** Os `tests/unit/test_*_endpoint.py` seguiram verdes sem uma linha editada.
  - **Por que eles seguiram verdes:** o `conftest` sobrescreve `get_link_repository` e `get_click_repository`, e o FastAPI resolve overrides pela assinatura do substituto — que não tem parâmetro. `get_session` fica *abaixo* deles na árvore e nunca é resolvido, então a suíte unitária não abre conexão nenhuma apesar de a aplicação estar ligada ao PostgreSQL.
  - **`test_dependencies.py` foi reescrito, não apagado.** Metade dele agora lê a árvore de dependências em vez de chamar coisas: uma sessão por requisição e o `scope="function"` são propriedades do grafo, não de valor de retorno.
  - **`in_memory_repositories.py` e seu teste removidos com `git rm`** (296 linhas), em commit separado para o diff da troca ficar legível.
  - **O que exatamente a Fase 3 deixou para cá.** `src/url_shortener/adapter/persistence/in_memory_repositories.py` existe porque o critério da Fase 3 exige o `uvicorn` de pé e um passeio com `curl`, e os fakes moram em `tests/`, que o `src/` não pode importar. Ele nasceu com data de validade escrita na docstring.
  - **A troca é literalmente isto:** `get_link_repository` e `get_click_repository` param de devolver `InMemoryLinkRepository`/`InMemoryClickRepository` e passam a devolver `LinkRepositoryImpl`/`ClickRepositoryImpl` ligados a uma sessão com escopo de requisição; o `@lru_cache` dos dois **sai** junto, porque ele existia só para o store em memória ser um singleton do processo — mantido, ele entregaria a mesma sessão para todas as requisições. O `@lru_cache` de `get_clock` fica: um relógio sem estado pode ser singleton.
  - **Depois disso, `git rm src/url_shortener/adapter/persistence/in_memory_repositories.py` e `tests/unit/test_in_memory_repositories.py`.** Deixar o arquivo vivo depois que ele não é mais usado é a forma mais fácil de a Fase 5 acabar testando o dicionário em vez do Postgres.
  - **O que confere se a troca foi feita direito:** `git diff --stat` do commit não pode tocar em **nenhum** arquivo de `adapter/web/`. Os testes de `tests/unit/test_*_endpoint.py` continuam verdes sem edição, porque eles sobrescrevem as portas de saída e não os casos de uso.
  - **A fronteira da transação tem que ser decidida aqui, e escrita.** A Fase 2 fixou que uma falha ao gravar o clique derruba o redirect, e a Fase 3 manteve. O formato usual — uma dependência `yield` que commita a sessão no código de saída — roda **depois** de a resposta já ter sido enviada, e transformaria essa decisão em falsa na prática: um `302` que não gravou nada. O commit precisa acontecer enquanto a resposta ainda pode mudar. Decidir isso na docstring do `dependencies.py` e numa ADR, antes de escrever o repositório.
  - **`Settings` passa a ser construído no boot.** Hoje ele é um `Depends`, então um processo sem `BASE_URL` sobe limpo e só quebra na primeira requisição. Com o `DATABASE_URL` de fato em uso, essa checagem vale: uma chamada a `get_settings()` dentro do `create_app()` resolve, e a suíte de API passa a precisar das variáveis (ou de um `create_app` que aceite `Settings`). Faz par com o `/health` desta mesma fase.
  - **O `/health` muda nesta fase e o teste que trava isso já existe:** `tests/unit/test_health_endpoint.py::test_health_needs_no_dependency_to_answer` afirma que a rota não declara dependência nenhuma. Ele **tem** que ser reescrito aqui, de propósito — é o marcador de que o endpoint deixou de ser estático e passou a rodar `SELECT 1`.
- **Critério:** ~~`docker compose up -d postgres`, `uv run alembic upgrade head`, e o mesmo passeio manual da Fase 3 funcionando contra o Postgres — **com diff vazio na camada `web`**.~~ **Atendido.**
  - **O diff vazio:** `git show --shortstat d4633c2` → `2 files changed`, nenhum em `adapter/web/`.
  - **O passeio, contra o Postgres real:** `POST /links` → `201` + `Location`; o mesmo `POST` de novo → `200`, mesmo código e **mesmo `created_at`** (é a linha, não uma recriação); `GET /{code}` → `302`; `GET /links/{code}` → `200` com `total_clicks: 3`; código inexistente → `404 link-not-found`; `169.254.169.254` como alvo → `400 invalid-target-url` com `reason: non-public-address`; `/health` → `200`. Repetido do zero depois das correções da revisão, porque o binário tinha mudado.
  - **O estado do banco, que o passeio de fora não alcança:** uma linha para a URL duplicada; 3 cliques com `ip` do tipo `inet` e `occurred_at` `timestamp with time zone`; `referer` `NULL` onde ausente; e `link_id_seq.last_value = 2` depois da URL recusada — prova de que a validação roda antes do `nextval`.
  - **Banco fora, aplicação de pé:** `/health` → `503 service-unavailable` em **3,04 s** (o `connect_timeout`, não os 30 s do `pool_timeout`); `GET /links/{code}` → `500 internal-error`. Depois do `docker compose start`, as duas voltaram **sem reiniciar a aplicação** — `pool_pre_ping`.
  - **Os gates:** `uv run pytest` → `482 passed` (3 s, sem Docker); `uv run mypy` → `Success: no issues found in 109 source files`; `ruff check .` e `ruff format --check .` limpos; `uv run lint-imports` → **`Contracts: 4 kept, 0 broken`** (o `.importlinter` não mudou, como a ADR-0004 já previa). `git diff --stat main..HEAD`: **43 arquivos, 2.534 inserções**.
  - **Prova de que a suíte não precisa de ambiente:** com o `.env` escondido, `uv run pytest` → `482 passed`. Com a alternativa ingênua (`get_settings()` dentro do `create_app`), → **erro de coleta**, a suíte inteira. É por isso que o `Settings` é resolvido no lifespan.
  - **Fora deste item:** Testcontainers, `tests/mothers.py`, o gate de cobertura e o job de integração no CI — todos da Fase 5. Até lá, o comportamento dos repositórios contra o PostgreSQL está verificado **à mão**, não por teste automatizado.
  - **Caveats — o que uma revisão adversarial derrubou depois de a fase estar "pronta".** 26 achados confirmados, e 3 deles eram defeitos de código, não de prosa. (1) O probe do `/health` usava a **mesma engine** das requisições, logo o mesmo `QueuePool`: um pool esgotado faria o endpoint esperar `pool_timeout` e depois culpar o banco. Corrigido com uma segunda engine `NullPool`. (2) `create_app(settings=...)` **não** impedia a aplicação de ler o ambiente — o `SettingsDep` chamava o leitor a cada requisição, e um `POST /links` sem `.env` respondia `500`; escondido porque o `conftest` também sobrescrevia aquele provider. (3) Nem o lifespan nem o provider real do probe tinham teste algum. O resto eram afirmações falsas em comentário e em ADR, corrigidas uma a uma. A mais interessante: o ramo `RuntimeError` do `create_link_use_case` **não** fica alcançável sob `REPEATABLE READ` — ali o próprio `INSERT` levanta `SerializationFailure` (medido contra o Postgres). Isso fortaleceu a decisão de fixar `isolation_level="READ COMMITTED"` na engine: sob o outro nível a deduplicação simplesmente não funciona.
- **Documento de aprendizado:** `docs/learning/fase-4-persistencia.md`

## Fase 5 — Testes de integração com Testcontainers

- [x] ~~`tests/conftest.py`: `PostgresContainer` com escopo de sessão, `alembic upgrade head` aplicado contra ele, e o client HTTP apontando para a aplicação com as dependências reais. Os testes rodam a mesma migration que a produção roda~~
  - **Feito, em `tests/integration/conftest.py` e não em `tests/conftest.py`.** O desvio é deliberado e já vinha da Fase 3, que dividiu a suíte em `tests/unit/` e `tests/integration/` com um `conftest` cada. Fixtures de Testcontainers na raiz ficariam visíveis para a suíte unitária, que é exatamente o que ela existe para não ter.
  - **Nada é sobrescrito. Zero `dependency_overrides`.** É a imagem espelhada do `tests/unit/conftest.py`: lá as três portas de saída são fakes e nada abre socket; aqui os repositórios são os `...Impl`, a sessão é a que o `get_session` abre, a transação commita onde a ADR-0007 diz, e o probe roda um `SELECT 1` de verdade.
  - **O schema vem da migration, nunca de `Base.metadata.create_all()`** — é a única coisa que testa a migration. O container é de sessão; o isolamento por teste é um `TRUNCATE link, click RESTART IDENTITY` autouse, sem `CASCADE` (as duas tabelas estão listadas, e o `CASCADE` truncaria em silêncio a tabela que alguém esquecesse de listar amanhã).
  - **`RESTART IDENTITY` é o que permite afirmar o código exato.** Todo teste começa em `0000001`, então uma asserção nomeia o código em vez de comparar dois códigos entre si — e um gerador que devolvesse o formato certo com o valor errado passaria no segundo caso.
  - **Duas decisões que não são default.** `PostgresContainer(..., driver="psycopg")`: o default é `psycopg2`, que este projeto não tem, e a falha chegaria como erro de import lá de dentro do SQLAlchemy. E o `Config()` do Alembic é construído **vazio** e preenchido, em vez de lido do `alembic.ini` — o `env.py` chama `fileConfig` sempre que recebe um arquivo, e `fileConfig` desliga todo logger que o ini não nomeia. Em produção é inofensivo (`alembic upgrade head` é processo próprio); aqui rodaria dentro do processo de teste e silenciaria o logger do `DatabaseProbe` pelo resto da sessão. **Medido:** depois da chamada, `logging.getLogger("url_shortener.adapter.persistence.database.probe").disabled` é `True`. E existe um teste que segura isso: a asserção de log no `/health` 503 reprova se o logger estiver desligado.
  - **Verificado:** `uv run pytest -m integration` → **21 passed em 10,49 s**. E a prova de que a suíte rápida não precisa de Docker: `docker ps -aq | wc -l` antes e depois de um `uv run pytest` cru → **28 e 28**, nenhum container subiu, 484 passed em 3,73 s.
- [x] ~~Teste 1, criar e seguir: `POST /links`, depois `GET /{code}`, afirmando `302` e o `Location`. É o caminho feliz de ponta a ponta~~
  - **Feito** em `tests/integration/test_create_and_follow.py`, cinco testes. Além do 201 → `0000001` → 302 → `Location`, ele lê a linha em `link` (`url` byte a byte, `url_hash` conferido contra `hash_url`) e o `Cache-Control: no-store`.
  - **`test_the_stored_timestamp_is_aware_and_close_to_now` é o único teste da suíte que pega uma regressão de `TIMESTAMPTZ` para `TIMESTAMP`.** O `alembic check` não pega: tirar `timezone=True` da entidade **e** da migration deixa as duas de acordo. Uma revisão adversarial tentou derrubar esse teste como vazio duas vezes; as duas foram refutadas com mutação medida — trocando a coluna para `timestamp without time zone`, ele reprova na linha do `utcoffset()`.
  - **`test_a_refused_target_spends_no_id_from_the_sequence`** fixa a ordem de duas linhas do `CreateLinkUseCaseImpl` que nada mais alcança: uma sequence não obedece a rollback, então validar depois do `next_id` queimaria um código a cada requisição recusada — invisível em toda resposta, e visível só aqui, no código que o link seguinte recebe.
- [x] ~~Teste 2, deduplicação: `POST` da mesma URL duas vezes, afirmando que o código devolvido é o mesmo **e** que existe exatamente uma linha em `link`~~
  - **Feito, em três níveis**, porque nenhum basta sozinho. Sequencial: os dois corpos são idênticos, o que diz que a linha existente voltou e não que um segundo link foi criado — o `created_at` é carimbado na criação e não se reproduz. Mais o **controle** que esse teste exige: duas URLs distintas viram duas linhas, sem o qual um repositório que respondesse toda busca com a primeira linha passaria na deduplicação e estaria catastroficamente errado.
  - **Verificado:** 4 testes em `test_deduplication.py`.
- [x] ~~Teste 3, o clique foi registrado: `GET /{code}` e depois uma linha em `click`. Prova o efeito colateral, não o retorno da função~~
  - **Feito**, em `test_click_is_recorded.py`. O `ip` é comparado com um `IPv4Address` e não com texto: a coluna é `INET` e o psycopg 3 registra loaders de `ipaddress` por padrão, então comparar com string afirmaria o contrato errado.
  - **`test_every_access_is_a_new_row_and_nothing_is_ever_updated` só ganhou dentes depois da revisão.** A cláusula *Then* prometia que a primeira linha ficava "exatamente como foi escrita" e o teste lia só ids — uma implementação que reescrevesse as linhas anteriores passaria. Agora o primeiro acesso é o único que manda `referer`, sua linha é fotografada, e a comparação depois de mais dois acessos reprova em qualquer reescrita. O `[REFERER, None, None]` está afirmado também, para que três linhas idênticas não satisfaçam a comparação por vacuidade.
  - **A decisão que nenhuma resposta revela** também virou teste: o `information_schema` fixa as colunas de `link` em exatamente cinco. Uma API com coluna contadora e uma que conta linhas respondem `total_clicks` igual.
- [x] ~~`tests/mothers.py` com Object Mother: classe final, construtor privado, factories estáticas de cenário, sem builders~~
  - **Feito.** `TargetUrlMother`, `LinkMother`, `ClickMother`. `@final` (cobrado pelo mypy, já que o interpretador não tem a palavra) e `__init__` que levanta — a tradução honesta de "construtor privado" para uma linguagem que não tem o conceito.
  - **Retrofit em 6 arquivos**, que era onde estava a duplicação real: `Link(id=n, code=ShortCode.from_id(n), url=..., created_at=...)` aparecia escrito à mão em `test_mappers.py`, `test_fakes.py`, `test_link_lookup.py`, `test_create_link_use_case.py`, `test_resolve_link_use_case.py` e `test_get_link_details_use_case.py`.
  - **Fora deste item, de propósito:** `test_link.py` e `test_click.py` montam os objetos à mão porque as recusas do próprio construtor são o que eles testam, e os testes de SQL compilam dicionários de colunas, não objetos de domínio. Mother em qualquer um dos dois esconderia justamente o que está sob teste.
  - **`LinkMother.with_a_mixed_case_code()` carrega o id `999_999_999_999` por um motivo:** seu código é `hBxM5A3`, então uma asserção sobre ele reprova contra uma fronteira que normaliza caixa. `0000001` não consegue, porque é só dígitos.
- [x] ~~Gate de cobertura no CI (`--cov-fail-under`), mais exigente sobre `domain/` do que sobre `adapter/` — um número único deixaria passar adaptador coberto com domínio furado, que é o inverso do que interessa~~
  - **Feito, e não com `--cov-fail-under`.** Esse flag é um número só. Dois `coverage report` sobre o **mesmo** `.coverage` dão dois gates: `--include="src/url_shortener/domain/*,src/url_shortener/application/*" --fail-under=100` e `--fail-under=100` na árvore inteira.
  - **Os dois estão em 100, e a diferença entre eles não é o número: é o que pode ser abaixado.** O gate do domínio roda no job rápido, é alcançável sem banco nenhum — então um furo lá nunca é "a infraestrutura não subiu", é uma regra que ninguém testou — e não se negocia. O global mora no job de integração e é o que um PR pode argumentar para baixo explicando por quê.
  - **Sem folga, e com válvula de escape escrita:** linha genuinamente inalcançável leva `# pragma: no cover` com o motivo, o que põe a exceção no diff onde um revisor vê, em vez de escondê-la dentro de um limiar que ninguém revisita.
  - **Verificado:** `uv run pytest -m ""` → **715 statements, 92 branches, zero perdidos**. Dois testes unitários pequenos fecharam o que faltava: o corpo de `get_settings()` (o caminho de produção — `create_app()` sem argumento — que nada exercitava) e o ramo do `_describe_errors_accurately` que preserva um schema ainda referenciado.
- [x] ~~O CI ganha o job de integração. O `ubuntu-latest` já tem Docker, então o workflow **não** declara `services:`: o teste sobe o próprio container~~
  - **Feito.** Dois jobs, **sem `needs:` entre eles**: um erro de lint e um redirect quebrado são fatos independentes, e um PR que tem os dois deve ouvir os dois de uma vez.
  - **Nenhum `services:` em lugar nenhum do arquivo.** O container é declarado pelo código que precisa dele, na versão que esse código fixa, e nada no CI precisa ser mantido em dia com o `compose.yml` à mão.
  - **`-m ""` roda as duas suítes num processo só**, e não por conveniência: nenhuma das duas alcança o gate sozinha. A unitária perde exatamente doze statements — o `get_session` e os dois repositórios — e a de integração perde todo caminho de erro que os fakes dirigem.
- **Critério:** ~~`uv run pytest -m integration` verde local e no CI, e um pull request com teste vermelho fica bloqueado. Provar a não-vacuidade do gate de cobertura removendo uma **classe de teste inteira** e vendo o CI reprovar.~~ **Atendido localmente. A metade "no CI" depende do push e do PR, que são do usuário.**
  - **Os gates:** `uv run pytest` → 484 passed (3,73 s, sem Docker); `uv run pytest -m integration` → 21 passed (10,49 s); `uv run pytest -m ""` → **505 passed**; `uv run mypy` → `Success: no issues found in 116 source files`; `ruff check .` e `ruff format --check .` limpos (128 arquivos); `uv run lint-imports` → **`Contracts: 4 kept, 0 broken`** — o `.importlinter` não mudou.
  - **`git diff --stat main..HEAD`: 18 arquivos, 1.387 inserções, em 10 commits.**
  - **A não-vacuidade dos gates, medida por deleção — e a terceira linha fica no registro porque é a mais honesta:**

    | Removido | Número | Saída |
    |---|---|---|
    | `tests/unit/test_url_policy.py` | domínio 90% | **exit 2** |
    | `tests/integration/` inteiro | árvore 99% | **exit 2** |
    | `tests/integration/test_click_is_recorded.py` | árvore **100%** | exit 0 |

    A terceira linha é exatamente a armadilha que este item do roadmap já antecipava. Cobertura de linha é um instrumento fraco: aquele módulo pode ser apagado sem mover o número, porque o `total_clicks` do teste 1 e o teste de falha no commit passam pelas mesmas linhas de `record` e `count_by_link`. **O gate pega uma camada ficar sem teste, não um cenário sumir.** Para cenário, o que serve são os testes.
  - **Extras do roadmap entregues nesta fase** — os quatro estavam em "Extras, se sobrar tempo" ou adiados pela Fase 4:
    - **O teste de concorrência**, que o roadmap chama de o argumento inteiro do projeto num teste só: 8 requisições soltas juntas por um `threading.Barrier` → um `201`, sete `200`, um código, uma linha. **Medido 60 vezes antes de ser commitado:** 7 ou 8 dos 8 pedidos passaram do `SELECT` em todas elas, contra um limiar de 2 — que é a asserção que impede o teste de passar por vacuidade.
    - **A corrida perdida, determinística**, com duas `Session` reais e a janela aberta à mão entre os passos 2 e 3. É a única versão que exercita o ramo perdedor em toda execução; a concorrente prova a invariante, mas num escalonamento infeliz todos poderiam pegar o caminho rápido.
    - **Envenenar o `COMMIT`** — o que a ADR-0007 nomeia como "a versão da Fase 5 desta verificação". Um `UNIQUE (link_id) DEFERRABLE INITIALLY DEFERRED` em `click` faz o segundo redirect responder **500, e não 302**. Tinha que ser adiado: qualquer coisa que fizesse o `INSERT` levantar seria pega dentro do caso de uso e não provaria nada sobre a fronteira. O PostgreSQL só adia `UNIQUE`, `PRIMARY KEY`, `FOREIGN KEY` e `EXCLUDE` — por isso é constraint de unicidade e não `CHECK`. Um segundo teste afirma que a conexão volta usável, para que um commit falho continue sendo uma requisição falha e não uma indisponibilidade.
    - **`/health` com o pool esgotado** — a ADR-0008 como fato observável, que também não tinha teste. As quinze conexões do pool de requisições são retiradas à mão e o endpoint responde `200` na hora, porque o probe roda numa engine `NullPool` que não tem fila para esperar.
  - **Caveats — o que uma revisão adversarial derrubou depois de a fase estar "pronta".** 15 achados em cinco lentes, **7 sobreviveram à refutação, e todos os 7 eram afirmação falsa em comentário — nenhum defeito de código.** O mais caro: os dois `conftest` diziam que `follow_redirects=False` existe porque seguir o 302 faria o test client bater na rede. **Medido: zero resoluções de DNS**, e o salto é atendido pelo catch-all *desta mesma aplicação* — o client despacha tudo pelo seu único transporte ASGI, qualquer que seja o host da URL. O flag continua necessário, por um motivo melhor: o salto vira `404` e enterra o `302` e o `Location` em `response.history`, onde as asserções que os nomeiam não os enxergam. A frase estava em `tests/unit/conftest.py` **desde a Fase 3** e foi corrigida junto. Os outros seis: o teste de concorrência não satura o pool (`RACERS = 8` contra capacidade 15 — quem satura é o de health), a suíte não depende de nenhuma semântica nova do PostgreSQL 18, a suíte unitária **já cobre quase todo o adaptador** (a lacuna são doze statements, e essa estreiteza é o fato interessante), um docstring nomeava a metade errada de um `and`, e a cláusula *Then* do teste de append-only prometia o que nenhuma asserção conferia.
  - **Fora desta fase:** exercitar `alembic downgrade` — precisaria de um banco próprio para ser seguro no meio da sessão, e não é o que a fase pediu. Testes de mutação sobre `domain/` seguem em `PROGRESS-V2.md`.
- **Documento de aprendizado:** `docs/learning/fase-5-testcontainers.md`, com o exercício em `docs/learning/exercicio_fase_5.py`.

## Fase 6 — Docker

- [x] ~~Dockerfile multi-stage: estágio de build com uv instalando as dependências em camada própria, estágio final slim, usuário não-root~~
  - **Feito, com uma decisão que não é a receita divulgada: os dois estágios têm o mesmo `FROM`.** O padrão comum usa `ghcr.io/astral-sh/uv:python3.13-bookworm-slim` no builder e `python:3.13-slim-bookworm` no final. Aqui os dois são `python:3.13-slim-bookworm` e o uv entra como binário (`COPY --from=ghcr.io/astral-sh/uv:0.12.7 /uv`). **O motivo é o que uma virtualenv é:** um `pyvenv.cfg` apontando para um interpretador por caminho absoluto — medido dentro da imagem, `home = /usr/local/bin`, `version_info = 3.13.15`. A venv copiada só funciona se os dois estágios tiverem o mesmo Python no mesmo lugar, e no padrão divulgado isso é uma coincidência entre duas tags, não uma garantia.
  - **`UV_PYTHON_DOWNLOADS=never` fecha o mesmo buraco pelo outro lado:** sem ele o uv pode baixar um interpretador próprio e construir a venv contra ele, produzindo uma venv que aponta para um Python inexistente no estágio final. A falha chegaria como `ImportError` no start do container, longe da causa.
  - **Duas camadas de dependência, na ordem que o cache cobra.** A primeira roda `uv sync --locked --no-install-project --no-dev` com `pyproject.toml` e `uv.lock` montados por `--mount=type=bind` — as entradas daquela camada são os dois arquivos e nada mais, e nada de `src/` está lá. A segunda copia o fonte e roda `uv sync --locked --no-dev --no-editable`. **`--no-editable` é o que permite o estágio final não carregar `src/`:** no modo padrão a venv guardaria um ponteiro para `/app/src`, que não existe na imagem final.
  - **O `COPY` do runtime não usa `--chown`, e essa é a correção de código que a revisão adversarial arrancou.** A primeira versão copiava a venv e as migrations com `--chown=app:app`, o que parece cuidado e é o contrário: dava ao processo permissão para **reescrever o código que ele executa**. Sem o flag os arquivos ficam de `root`, modo 0755/0644, e o usuário tem exatamente o que usa — ler e executar. **Medido nos dois sentidos:** antes, `touch` funcionava em `/app/.venv`, em `site-packages` e em `/app/migrations`; agora os três respondem `Permission denied`, e o `alembic upgrade head`, o `/health`, o `POST` e o redirect continuam idênticos.
  - **`.dockerignore` é allowlist, não denylist:** `*` e seis entradas liberadas por nome. Um denylist erra por omissão — o próximo arquivo com credencial criado na raiz entraria no contexto sem ninguém decidir. `!README.md` está lá por um motivo que não é documentação: o `pyproject.toml` declara `readme = "README.md"` e o hatchling não constrói o wheel sem ele. E `**/__pycache__` vem **depois** das inclusões, porque a última regra que casa vence.
  - **`CMD` em forma exec, sem `sh -c`,** para o uvicorn ser o pid 1 e ser avisado de que deve parar. **Medido dos dois lados:** esta imagem para em **1,44 s**, com `Finished server process [1]` no log; um container `sh -c "sleep 300 && echo never"` leva **10,65 s** e sai com **137** — o kernel não entrega a um pid 1 sinal cuja disposição é a padrão, então o shell nem recebe o `SIGTERM`, quanto mais repassa.
  - **`HEALTHCHECK` com `urllib` da stdlib**, porque a imagem slim não tem `curl` e instalar um pacote para o healthcheck seria pagar superfície de ataque por conveniência. `urlopen` levanta em `503`, o que dá saída não-zero. **Medido com o postgres derrubado: `unhealthy` em t+30 s** (10 s de `start-period` mais três tentativas), e `/health` de volta a `200` na primeira chamada depois de o banco voltar.
  - **Verificado:** imagem **303 MB**; contexto de build de **6 entradas, 654 KB** (contra 168 MB e 6.619 arquivos só de `.venv`); `docker run --rm url-shortener:0.1.0 id` → `uid=10001(app)`; `python -c "import pytest"` → `ModuleNotFoundError`, que é a prova do `--no-dev`; `url_shortener.__file__` → `/app/.venv/lib/python3.13/site-packages/...`, e `/app` contém `.venv`, `alembic.ini` e `migrations` e nada mais; `ls -R /app/migrations` sem nenhum `__pycache__`.
  - **Fora deste item:** pin por digest em vez de tag, imagem distroless, `hadolint` no CI e qualquer redução de tamanho além do `slim`. A tag `python:3.13-slim-bookworm` continua flutuando dentro do patch, que é o preço aceito para não atualizar um digest à mão a cada CVE.
  - **Caveat:** o `HEALTHCHECK` repete a porta `8000` porque não enxerga a do `CMD`. Mudar uma exige mudar a outra.
- [x] ~~`compose.yml` completo, com `api` e `postgres`, aplicando a migration antes de o servidor subir~~
  - **Feito com três serviços, e não dois.** `postgres`, `migrate` e `api`. O `migrate` é a mesma imagem da `api` com `command: ["alembic", "upgrade", "head"]`: roda e sai. A `api` declara duas condições — `postgres: service_healthy` e `migrate: service_completed_successfully` — e a segunda é o mecanismo inteiro da fase. **ADR-0009.**
  - **A prova de que a condição é sobre código de saída, e não sobre tempo:** com a DSN do `migrate` apontada para um banco inexistente, `migrate` sai com **1**, a `api` fica em **`Created`** sem nunca iniciar, nada escuta na 8000, e `docker compose up --wait` **sai com código 1** — que é o que reprova o job do CI. Com a migration no startup da API, o mesmo erro seria um container reiniciando em laço e respondendo `500` sobre um schema ausente.
  - **Um anchor YAML (`x-app-environment`) para o ambiente**, de modo que a DSN exista uma vez só em vez de duas cópias que divergem.
  - **Duas coisas que só aparecem quando se roda:** o serviço `migrate` precisa de `BASE_URL`, que ele nunca usa, porque `migrations/env.py` chama `get_settings()` e `Settings` proíbe chave extra e não tem default — a migração valida a configuração inteira da aplicação. E `environment:` explícito em vez de `env_file: .env`, porque o `.env` de quem seguiu o `.env.example` aponta para `localhost:5432`, que dentro do container é o próprio container.
  - **`healthcheck: disable: true` no `migrate`:** a imagem declara um check HTTP que faz sentido para a API e nenhum aqui, já que este container nunca sobe servidor. Desligado, e não herdado e ignorado.
  - **Verificado:** partindo de `docker compose down -v`, `docker compose up -d --wait` em **21 s** com a imagem pronta e **32 s** incluindo o build, código de saída **0**; `docker compose ps -a` com `migrate  Exited (0)` **antes** de a `api` existir; o log do `migrate` com `Running upgrade  -> cee192947f04`; o fluxo respondendo `201` → `302` com o `Location` certo → `total_clicks: 1`, e o `psql` mostrando **uma** linha em `link` e **uma** em `click`, com `ip` `172.26.0.1` na coluna `INET`.
  - **Fora deste item:** política de restart, limites de recurso, réplicas, rede nomeada e serviço de `downgrade`. Reverter uma migration continua sendo comando na mão, o que é adequado com uma revisão só e vira dívida na décima.
  - **Caveat:** a porta `5432` continua publicada em todas as interfaces da máquina, e não só em loopback. Para um banco de laptop com credencial de desenvolvimento é aceitável; o comentário no arquivo agora **diz isso**, em vez de chamá-la de "porta local".
- [x] ~~O CI ganha o `docker build`~~
  - **Feito, e faz mais do que `docker build`.** Um terceiro job, `image`, **sem `needs:`** dos outros dois, pela mesma razão que os dois já eram independentes. Ele constrói a imagem, sobe os três serviços com `up -d --wait` e executa o **Critério da fase**: `POST /links` afirmando `201`, `GET /{code}` afirmando `302` e o `Location`, e `GET /links/{code}` afirmando `total_clicks == 1` e `short_url`.
  - **Os dois jobs antigos rodam o código; este roda o artefato.** `docker build` sozinho provaria que a imagem compila — não que a migration rodou antes do servidor, não que o usuário não-root consegue ler o que precisa, e não que `BASE_URL` chega na resposta. A última só passou a ser verdade quando a revisão apontou que nada no job lia `short_url`; agora lê, do mesmo corpo que já estava em mãos, e é a única parte da resposta que depende do `compose.yml`.
  - **O redirect é pedido uma vez só**, com `%{http_code}` e `%{redirect_url}` saindo da mesma requisição. É o que faz `total_clicks == 1` afirmar "um acesso, uma linha" em vez de "algum número maior que zero".
  - **Deduplicação não é afirmada aqui**, de propósito: a suíte de integração já a cobre contra um PostgreSQL de verdade, inclusive sob concorrência, e repeti-la aqui custaria tempo sem dizer nada sobre a imagem.
  - **O script fica inline no workflow, e não em `scripts/smoke.sh`**, porque `scripts/` não existe no layout que o `CLAUDE.md` fixa. O custo é honesto: não há um comando único para rodar o smoke test localmente.
  - **Não há passo de teardown**, e isso é decisão e não esquecimento: o runner é destruído com o job. O `docker compose down -v` que importa é o local, e está no README.
  - **Verificado:** o script do job rodado verbatim contra a stack local → `code=0000001 status=302 clicks=1 short_url=http://localhost:8000/0000001`; `docker compose up -d --wait` medido em **0** com a stack sã e **1** com a migration quebrada; o YAML relido depois de cada edição (`jobs: check, integration, image`).
- **Critério:** ~~num clone limpo, `docker compose up` e o fluxo `POST /links` seguido de `GET /{code}` funciona com um comando só, sem nenhum passo manual.~~ **Atendido localmente. A metade "no CI" depende do push e do PR, que são do usuário.**
  - **Os gates:** `uv run pytest -m ""` → **505 passed**; `uv run coverage report` → **715 statements, 92 branches, zero perdidos** nos dois gates; `uv run mypy` → `Success: no issues found in 116 source files`; `ruff check .` e `ruff format --check .` limpos (129 arquivos); `uv run lint-imports` → **`Contracts: 4 kept, 0 broken`**. **Nenhuma linha de `src/` mudou nesta fase**, e nenhum número acima se moveu — o que é, em si, o resultado: empacotar não pediu uma linha de aplicação.
  - **O fluxo, de fora, num banco recém-criado:** `POST /links` → `201` com `code: "0000001"` e o `Location` dos metadados; `GET /0000001` → `302` com `cache-control: no-store` e `Location` para a URL longa; `GET /links/0000001` → `total_clicks: 1`; o mesmo `POST` de novo → `200`, com uma linha em `link`.
  - **`git diff --stat main..HEAD`: 7 arquivos, 466 inserções, 18 remoções, em 6 commits.**
  - **Caveats — o que uma revisão adversarial derrubou depois de a fase estar "pronta".** 23 achados em cinco lentes, 10 verificados por um refutador cada, **3 sobreviveram — e desta vez um era defeito de código, não de prosa.**
    - **O defeito:** os três `COPY --chown=app:app` davam ao usuário do runtime permissão de escrita sobre a `.venv`, sobre `site-packages` e sobre `migrations/` — e o comentário duas linhas acima afirmava exatamente o contrário ("the user below cannot write to `/app`"). Só o diretório `/app` pelado era de `root`. Corrigido removendo os três flags, com o comportamento medido idêntico antes e depois.
    - **A segunda:** a receita "Sem Docker, para desenvolver" do README falhava no terceiro comando num clone limpo, porque nenhum passo criava o `.env` e `Settings` não tem default.
    - **Seis afirmações falsas mais**, vindas da lista que o ranking deixou sem verificar e conferidas uma a uma antes de entrar: o cabeçalho do `ci.yml` ainda abria com "Two jobs" três linhas acima da frase que acrescenta o terceiro; o comentário do smoke test contava "duas chamadas `curl`" num passo que faz três; o mesmo comentário prometia provar que `BASE_URL` chega na resposta sem nada ler `short_url` (resolvido acrescentando a asserção, não apagando a frase); a ADR dizia que o `sh` "sai" ao receber o `SIGTERM`, e a medição mostra que ele **não sai** — é justamente por isso que os dez segundos acontecem; o comentário do `postgres` chamava de "porta local" uma porta publicada em todas as interfaces; e o cabeçalho do Dockerfile contrastava o estágio final por não ter "uv nem compilador", quando nenhum dos dois estágios tem compilador, já que ambos são `slim`.
    - **O que sobreviveu à refutação e não virou mudança:** nada. As sete refutadas incluem duas que valiam a pena ouvir — `UV_PYTHON_DOWNLOADS=never` acusado de não mudar nada neste build (é guarda, não efeito) e o `timeout=3` do healthcheck acusado de empatar com o `connect_timeout` da aplicação (com o banco parado a conexão é recusada na hora, e o `503` chega bem antes).
- **Documento de aprendizado:** `docs/learning/fase-6-docker.md`, com o exercício em `docs/learning/exercicio_fase_6.py`.

## Fase 7 — Documentação e acabamento

- [x] ~~`README.md` em português: o problema em três frases, o diagrama do fluxo em Mermaid, `docker compose up`, a **seção de decisões** e a **tabela do que ficou de fora** com a resposta pronta para cada corte. Essa tabela é a parte do repositório que um entrevistador técnico lê primeiro~~
  - **Feito, e a fase cresceu de um arquivo para oito.** O item pedia um README; o que foi construído foi um conjunto de documentação com o README como **hub**, no formato do projeto irmão `shopflow`: cada seção é um resumo denso que termina apontando para o documento que carrega o detalhe. `README.md` (313 linhas) mais sete arquivos novos em `docs/` — `README.md` (índice), `CHALLENGE.md`, `ARCHITECTURE.md`, `API.md`, `DEVELOPMENT.md`, `TESTS.md` e `SECURITY.md`, **2.151 linhas**.
  - **A tabela "o que ficou de fora" mora no `CHALLENGE.md` e só nele.** Ela nasceu no README em três blocos e foi movida por decisão do usuário, na revisão da fase. A mudança de lugar apagou uma duplicação que ninguém tinha visto: o bloco das peças de escala do desenho do Xu era uma segunda versão, mais curta, da tabela que o `CHALLENGE.md` §4 já carregava — então ele foi **removido** em vez de mudado de lugar, e o §5 aponta para o §4. Sobraram dois blocos, funcionalidade e rigor/operação. É o que o `PROGRESS-V2.md` exige — as duas coisas andam juntas, e uma segunda cópia seria uma segunda coisa para manter verdadeira.
  - **Dois diagramas Mermaid, e nenhum repetido.** O README leva um hexágono compacto de três caixas e um `flowchart` do produto inteiro — os dois fluxos com a corrida perdida, o `201` versus `200` e o `INSERT` em `click` anotados. O `ARCHITECTURE.md` leva o hexágono detalhado, os dois fluxos em `sequenceDiagram` e o `erDiagram` do schema. Níveis de detalhe diferentes, e não duplicação.
  - **Badges:** o do CI aponta para o workflow real (`wastecoder/python-url-shortener`); o de cobertura diz **100%** porque o gate é `--fail-under=100`, e não porque soa bem.
  - **`SECURITY.md` entrou contra a intuição inicial**, e é o documento com mais conteúdo próprio: a `url_policy` já implementada recusa `javascript:`, `file://`, `data:`, credencial na URL, `localhost` e sufixos internos, toda faixa não roteável — incluindo `169.254.169.254` — e as quatro grafias de IPv6 que carregam um IPv4. Tem uma seção **"o que não está defendido"** com oito itens, que é a metade que importa numa entrevista.
  - **`OBSERVABILITY.md` foi avaliado e recusado.** Não existe métrica, tracing nem log estruturado neste projeto; o documento seria o único aspiracional num repositório cuja ética é não afirmar o que não é verdade. A ausência virou linha da tabela de cortes.
  - **Nenhum exemplo foi escrito de cabeça.** Os doze corpos de resposta do `API.md` foram **capturados da aplicação rodando** — um script com `TestClient`, relógio congelado e `BASE_URL=http://localhost:8000` exercitou quinze cenários e imprimiu status, headers e corpo. E as 29 linhas das tabelas de recusa do `SECURITY.md` saíram de **68 URLs executadas contra a `url_policy` real**: motivo e mensagem são a saída literal.
  - **Uma coisa que só aparece capturando:** o `total_clicks` do exemplo de `GET /links/{code}` é `1`, e é `1` porque o cenário anterior seguiu o redirect. Escrevendo de cabeça, o instinto é pôr `0`. Do mesmo jeito, a passagem das 68 URLs mostrou que `http://exemplo.br/ç` é **aceito** — a checagem de ASCII é sobre o host, não sobre o caminho.
  - **Verificado:** `uv run pytest -m ""` → **505 passed**; os dois gates → **100%** (317 statements / 72 branches em `domain`+`application`, 715 / 92 na árvore inteira); `ruff check`, `ruff format --check` (**136 arquivos**, que já incluem os documentos novos), `mypy` e `lint-imports` limpos; e um checador escrito para esta fase resolveu **242 links relativos e âncoras** em 19 arquivos, todos existentes.
  - **Revisão do usuário depois da primeira entrega, e ela mudou a forma do README.** Quatro pedidos: o fluxo na prática explicado passo a passo, em vez de um bloco de `curl` mudo; a tabela de cortes e a dos ADRs **fora** do README, cada uma no único lugar que já era dono do assunto; enumerações em prosa viradas lista ou tabela (os três serviços do `compose`, os três jobs do CI, a contagem de testes do `TESTS.md`); e no `API.md`, o exemplo de Swagger ao lado do de `curl`, com `#### Requisição` e `#### Resposta` separando os dois. O README encolheu de 342 para 313 linhas explicando **mais**.
  - **Uma pergunta do usuário virou seção do `API.md`, e a resposta estava no banco.** O `GET /{code}` "não funcionava" pelo Swagger, e não era bug. Colando a URL curta inteira no campo `code`, o caminho vira **dois segmentos** e `/{code}` casa com **um** — o `404` que voltava trazia `"type": "http-error"`, que é o roteador recusando, e não `link-not-found`. Colando só o código, o Swagger diz `Failed to fetch` porque o `fetch` dele segue o `302` sozinho e o destino é **outra origem**, que não manda `Access-Control-Allow-Origin`. **Medido:** um `SELECT` em `click` na stack de pé mostrava **sete linhas com `User-Agent` de Chrome** — a API tinha respondido `302` e gravado o clique em **toda** tentativa. O que falhava era o navegador, depois da resposta.
  - **Fora deste item:** `postman/` (para quatro rotas com `curl` de uma linha é cerimônia, e o `/docs` já é a coleção) e renomear `PROGRESS-V1/V2` para o padrão `PROGRESS`/`FUTURE-PROGRESS` do shopflow (quebraria links internos, o `CLAUDE.md` e as referências no histórico).
  - **Caveat:** `ruff format` processa Markdown, então os blocos Python dos documentos são gateados pelo CI. Os desta fase são três — o `get_session` no `ARCHITECTURE.md` e dois no `TESTS.md` — e os das ADRs 0005 e 0007 já eram gateados desde as fases que as escreveram. Todos formatados.
- [x] ~~Revisar os três ADRs escritos na Fase 0 contra o que foi de fato construído, e acrescentar um quarto se alguma decisão estrutural tiver mudado no caminho~~
  - **O item estava desatualizado, e o trabalho real era outro.** Não existem três ADRs: existem **nove**. Os 0001–0003 são da Fase 0 e os 0004–0009 nasceram nas Fases 3 a 6. Então não havia "um quarto" a escrever — havia nove a auditar contra o código.
  - **Auditoria adversarial dos nove, afirmação por afirmação, e cinco eram falsas.** Todas em contagens que envelheceram quando o código passou por cima delas, e **todas as correções foram medidas, não raciocinadas**:
    - **ADR-0003 dizia que o `compose.yml` tem dois serviços.** Tem três — `postgres`, `migrate` e `api` — desde a Fase 6. A contagem era a evidência de "nada de broker para manter", então agora ela nomeia o terceiro e diz por que ele não é um.
    - **ADR-0006 contava cinco handlers, cinco tipos de problema e quatro `type: ignore[arg-type]`.** São seis, seis e cinco. **Medido:** removendo os cinco `ignore`, `uv run mypy` acusa `Found 5 errors in 1 file`, nas linhas 54–57 e 60 e em nenhuma outra.
    - **ADR-0006 dizia que três testes reprovam sem `_describe_errors_accurately`.** **Medido**, trocando a chamada por `app.openapi_schema = app.openapi()`: `4 failed, 480 passed`. O quarto é `test_the_health_document_advertises_both_answers`, que a própria ADR-0008 criou ao documentar o `503` do `/health`. Os quatro estão nomeados agora.
    - **ADR-0008 descrevia a proporção como quatro `ignore` em cinco handlers**, que era o estado *antes* da mudança que ela própria descreve.
  - **Nenhum ADR novo, por decisão do usuário.** Três candidatos foram levantados e recusados: o modelo síncrono, "quem garante a invariante é o banco" e as settings/engine no lifespan. O primeiro é a lacuna honesta registrada nos objetivos de aprendizado.
  - **Uma imprecisão foi levantada e não virou mudança:** a ADR-0007 diz "o FastAPI 0.141.1 que este projeto trava", e o `pyproject.toml` declara `>=0.141.1`, que é piso. Quem trava é o `uv.lock`, e o `--locked` do CI e do Dockerfile é o que o torna efetivo — a frase continua verdadeira.
  - **Verificado:** ver o commit `docs(adr): correct the counts an audit measured as wrong`, com as medições no corpo da mensagem.
- [x] ~~Revisão do histórico: mensagens legíveis, nenhum commit gigante, nenhum trailer de co-autoria~~
  - **Limpo, e verificado por comando e não por leitura.** `git log --all` não tem **nenhuma** linha começando com `Co-Authored-By:`, `Signed-off-by:` ou `Generated with`, e nenhuma menção a Claude ou Anthropic fora de referências ao arquivo `CLAUDE.md`. **99 commits** quando a auditoria rodou, um autor só — 109 com os desta fase.
  - **Os 99 assuntos casam com o padrão Conventional** — `git log --format='%s' | grep -vE '^(feat|fix|refactor|test|docs|build|ci|chore|perf|style)(\([a-z]+\))?: [a-z]'` não devolve nada.
  - **Nenhum commit gigante.** Os dois maiores são `chore: initial commit` (13 arquivos, 2.476 inserções, **todas de roadmap e configuração**) e `chore: add the hexagonal package skeleton` (21 arquivos, **101 inserções**, que são 21 `__init__.py`). Os dois são um conceito só. 26 dos 99 carregam corpo além do assunto.
  - **Um achado real, e a correção foi no `CLAUDE.md` e não no histórico:** o vocabulário de escopos usado (`adr`, `progress`, `github`, `docker`, `compose`, `alembic`, `integration`, `lint`, `mypy`, `imports`, `packaging`) é mais largo que a lista de sete que o `CLAUDE.md` declarava — e os **exemplos do próprio arquivo** já usavam `docs(adr)`, `ci(github)` e `build(uv)`, fora da lista. A lista nunca foi fechada; agora ela diz isso. **Caveat:** `build(uv)` é exemplo do `CLAUDE.md` e **não** existe no histórico; a primeira versão desta lista o copiou de lá e omitiu `integration`, que existe. Corrigido pela revisão adversarial.
  - **Nada foi reescrito.** A `main` já foi publicada, então um histórico com violação seria reportado e não reescrito. Não houve violação.
- [x] ~~Marcar em `Objetivos de aprendizado` tudo que você já explica sem consultar, e listar honestamente o que faltou~~
  - **As caixas continuam vazias, de propósito.** O critério da lista é "explicar sem consultar nada", que é auto-avaliação do usuário; marcar por ele transformaria a lista numa mentira. O que a fase entregou foi o **mapa de evidências**: cada um dos 24 objetivos ganhou uma linha `→` dizendo onde a resposta vive hoje — arquivo, teste, ADR e seção do documento novo — para o ensaio ser conferir em vez de procurar.
  - **A lista honesta do que faltou tem três itens**, e o primeiro é o que mais vale: **o modelo síncrono é o único objetivo sem ADR e sem teste**. Nada no repositório reprova se um `async def` aparecer num controlador, e essa é justamente a decisão mais fácil de quebrar em silêncio, porque um `async def` isolado *compila*. Os outros dois: a lista de códigos reservados não tem chamador em `src/` (correto e documentado, mas defendido por argumento e não por comportamento), e a metade "no CI" de três critérios de fase segue não verificada.
- **Critério:** ~~um clone limpo sobe com um comando, e o README responde sozinho as seis coisas que sustentam o projeto: Testcontainers verificando estado do banco, a constraint fechando a corrida da deduplicação, 302 versus 301, `click` append-only, as peças do desenho do Xu que saíram, e a tabela de cortes com a justificativa de cada um.~~ **Atendido, com uma releitura do "sozinho" que precisa ser dita.**
  - **O README não tem seção de decisões nem tabela de cortes**, por decisão do usuário na revisão da fase: a tabela dos nove ADRs mora no `docs/README.md` e a de cortes no `CHALLENGE.md`, cada uma exatamente uma vez. Então as seis respostas **não** se concentram numa seção — quatro caem nas seções normais do README, e duas ficam a um link nomeado no ponto em que a pergunta aparece.

    | O que o critério exige | Onde a resposta está |
    |---|---|
    | Testcontainers verificando estado do banco | README, §Testes e qualidade — a tabela dos seis testes que carregam o projeto |
    | A constraint fechando a corrida | README, §O fluxo — o `ON CONFLICT` e o ramo do perdedor estão **no diagrama**, com três parágrafos abaixo |
    | 302 versus 301 | README, §O fluxo, segundo parágrafo, com o `307` junto |
    | `click` append-only | README, §O fluxo, terceiro parágrafo, e o `INSERT INTO click` no diagrama |
    | As peças do Xu que saíram | `CHALLENGE.md` §4, linkado da §Visão geral do README |
    | A tabela de cortes | `CHALLENGE.md` §5, linkado da mesma frase |
  - **`docker compose up` num clone limpo continua sendo um comando só**, e isso não mudou nesta fase: nenhuma linha de `src/`, de `compose.yml`, do `Dockerfile` ou do `ci.yml` foi tocada. **Toda a Fase 7 é documentação**, e é por isso que os números da Fase 6 seguem idênticos.
  - **Os gates:** `uv run pytest -m ""` → **505 passed**; `uv run coverage report` → **100%** nos dois escopos, zero statements e zero branches perdidos; `uv run mypy` → `Success: no issues found in 116 source files`; `ruff check .` e `ruff format --check .` limpos em **136 arquivos**; `uv run lint-imports` → **`Contracts: 4 kept, 0 broken`**.
  - **Caveats — o que uma revisão adversarial derrubou depois de a fase estar "pronta".** Seis leitores sobre os documentos novos, um refutador para cada, **807 afirmações verificadas contra o código** e 39 achados; **35 sobreviveram à refutação, e todos eram afirmação falsa em documento — nenhum defeito de código.** Os que mais custaram:
    - **A `main` não exige o que o README dizia que exige.** Medido com `gh api`: os contextos obrigatórios são **`check` e `integration`**, e o `image` **não** está entre eles. Ele roda em todo pull request e não barra merge nenhum — então "um pull request vermelho não é mergeável" era falso justo para o job que a frase anterior acabava de apresentar como o que sobe a stack inteira. Corrigido no `README.md`, no `DEVELOPMENT.md` e no `CLAUDE.md`, e a folga ficou registrada em vez de escondida.
    - **Dois comandos publicados não funcionavam.** O `TESTS.md` e o `DEVELOPMENT.md` mandavam rodar `uv run pytest -m ""` e depois `uv run coverage report --fail-under=100`. **Sem `--cov` a rodada não grava nada**, e o segundo comando responde `No data to report.` — ou, pior, julga um `.coverage` de outra rodada. Medido, e corrigido nos dois.
    - **Os números de passo dos diagramas apontavam para as setas erradas.** O `autonumber` do Mermaid conta cada mensagem, e o `SELECT` do perdedor é o passo **20**, não 12; o `INSERT ... ON CONFLICT` é o **15**, não 9. A frase mais afiada da seção — a que nomeia o bug que a constraint existe para matar — apontava para a resposta `200 OK`.
    - **Três explicações estavam certas na conclusão e erradas no mecanismo.** Sem o `_is_hostname`, `http://127.0.0.1\` **ainda** seria recusado (pela regra do rótulo final); quem passaria direto são o host percent-encoded, o rótulo vazio e o hífen na ponta. `2130706433` e `0x7f000001` caem na regra de **rótulo único**, e não na do rótulo final. E o `is_global` **não** vê através da 6to4: ele recusa o bloco inteiro, inclusive `2002:808:808::`, que carrega `8.8.8.8` — é o desembrulho que acerta os dois sentidos.
    - **Quatro contagens minhas.** "Quinze corpos capturados" são onze no documento (quinze eram os cenários do script); "68 URLs da tabela" são 29 linhas de tabela vindas de 68 URLs executadas; a porta `8000` aparece **três** vezes no Dockerfile e não duas; e o escopo `build(uv)` que eu listei como usado no histórico **não existe** nele — é exemplo do `CLAUDE.md`, e o que existe e faltava era `integration`.
    - **Cinco erros pré-existentes no `CLAUDE.md`**, herdados e não introduzidos aqui, corrigidos porque a fase já estava editando o arquivo: um gate de cobertura onde há dois, `compose.yml # api + postgres` onde há três serviços, um `tests/conftest.py` que não existe, "o teste que vale a pena escrever se sobrar tempo" para um teste escrito na Fase 5, e a seção de CI descrevendo `docker build` onde o job faz `up --wait` e um smoke test.
    - **O que não sobreviveu à refutação:** quatro achados, entre eles a acusação de que o `README.md` inventava a proporção de testes e a de que o `CHALLENGE.md` atribuía ao Xu algo que ele não diz.
  - **`git diff --stat main..HEAD`: 13 arquivos, 2.375 inserções, 67 remoções, em 9 commits** no momento em que este registro foi escrito; o décimo commit é ele mesmo, e o décimo primeiro é o das correções acima.
- **Documento de aprendizado:** `docs/learning/fase-7-documentacao.md`, com o exercício em `docs/learning/exercicio_fase_7.py`.

## Extras, se sobrar tempo

Em ordem de valor, do maior para o menor:

- [x] ~~**O teste de concorrência.** Duas requisições simultâneas com a mesma URL longa, afirmando que exatamente um link foi criado. É impossível de escrever com mock, porque o que está sob teste é o comportamento do banco sob concorrência — é o argumento inteiro do projeto num teste só~~
  - **Feito na Fase 5**, com oito e não duas: `test_eight_simultaneous_requests_for_one_url_create_exactly_one_link`, em `tests/integration/test_deduplication.py`. Um `201`, sete `200`, um único código, uma única linha em `link`.
  - **`threading.Barrier` e não oito threads num laço.** Num laço a primeira termina antes de a última começar, o que não é corrida nenhuma; a barreira segura todas até as oito chegarem e solta juntas.
  - **A asserção que impede o teste de passar por vacuidade:** mais de um id gasto da sequence. Se todas tivessem pego o caminho rápido, as outras quatro asserções continuariam verdes e nada teria corrido. **Medido em 60 execuções: 7 ou 8 dos 8 pedidos passaram do `SELECT` em todas elas**, contra um limiar de 2.
  - **Tem um irmão determinístico**, `test_the_loser_of_the_race_is_refused_without_raising_and_reads_the_winner`, com duas `Session` reais e a janela aberta à mão. A concorrente prova a invariante sob contenção de verdade; a determinística garante que o ramo perdedor roda em toda execução, o que a concorrente não pode prometer.
  - **Verificado:** ver o registro da Fase 5.
- [ ] Ensaiar as respostas dos `Objetivos de aprendizado` em voz alta, sem consultar
- [ ] A permutação multiplicativa que fecha a enumerabilidade — está descrita em [`PROGRESS-V2.md`](PROGRESS-V2.md), e só entra aqui se as Fases 0 a 7 estiverem inteiras
