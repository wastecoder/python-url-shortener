"""Domain entities and value objects, as frozen dataclasses.

`Link`, `Click` and `ShortCode` live here. They validate their own invariants on construction, so
an instance that exists is an instance that is valid.
"""
