"""Translation of domain errors into Problem Details (RFC 7807) responses.

`ProblemType` is the taxonomy and the handlers are the mapping. This is the only place in the
codebase that pairs an error with a status code.
"""
