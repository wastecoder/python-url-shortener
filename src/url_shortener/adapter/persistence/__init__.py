"""Driven adapter: PostgreSQL through SQLAlchemy.

The repository implementations live at this level; the SQLAlchemy models, the conversion to and
from the domain, and the session live in the packages below.
"""
