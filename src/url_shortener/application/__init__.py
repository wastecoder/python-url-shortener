"""Use cases and the ports they speak through.

Imports `domain` and the standard library, and nothing else. This layer answers "what can the
system do"; it never answers "over which protocol" or "into which database".
"""
