"""Fixtures shared by the integration tests.

Empty for now. From Fase 5 on, this is where the session-scoped `PostgresContainer` lives, along
with the Alembic upgrade that runs against it — the same migrations production runs.
"""
